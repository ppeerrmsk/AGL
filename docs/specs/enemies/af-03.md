---
id: af-03
kind: enemy
status: done
schema_version: 1
spec_version: 2
owner: design
depends_on: [railgun-weapon]
reconstruction_complete: true
---

# AF-03（电磁炮狙击无人机 · Schemer）

> 一架紫黑色高速无人机，远距（5–8 km）用电磁炮（railgun）狙击玩家：2 秒充能扇形预警 →
> 0.5 秒预测提前量 → 一发 hitscan 闪电（60 伤、穿透）→ 退到 8 km 外冷却 9 秒再来。
> 定位：远距压力源 + 走位/环境惩罚，逼玩家贴近到机炮距离（它近战无防御）。

## 1. 设计意图（Why）

- **体验目标**：制造一个"你必须冲进去"的远程威胁。AF-03 在 BVR 距离几乎无法被空空导弹拦
  （高速 + 退场），玩家学到的解法是**贴脸**——它没有近战手段。同时充能扇形是明确预警，给
  玩家"读招—变招/钻云/拉低空"的反应窗口。
- **Litmus 自检**（docs/DESIGN_PHILOSOPHY.md）：
  - 威胁有清晰预警相（2s 扇形收缩）+ 可被环境/走位降低命中 → 过"惩罚先给预告"。
  - 有明确正解（贴近到机炮距离）→ 过"可读可解"。
  - 不是隐形墙：射程/冷却/命中规则全部可学习。
- **反模式规避**：电磁炮**伤害恒定 60、不随等级缩放**（HP 缩放但火力不缩放）——避免后期变成
  "一发秒杀"的数值海绵式难度；难度来自距离与节奏，不是伤害膨胀。

## 2. 数据定义（What —— 全部数值，权威源）

### 2.1 本体基础属性（AircraftParams，`enemy_af03.tres`）

> 速度单位 km/h；距离 px（`PIXELS_PER_METER = 0.5`，1 px = 2 m）；高度 m。

| 字段 | 值 | 字段 | 值 |
|---|---|---|---|
| display_name | AF-03 | is_unmanned | false |
| max_hp | 90.0 | armor | 0.0 |
| max_speed | 2700.0 km/h | cruise_speed | 1100.0 km/h |
| stall_speed_base | 220.0 km/h | acceleration | 95.0 |
| deceleration | 110.0 | g_drag_factor | 2.4 |
| max_g | 10.0 | max_g_structural | 13.0 |
| roll_rate | 5.5 | max_altitude | 17000.0 m |
| climb_rate_max | 320.0 | thrust_to_weight | 1.35 |
| drag_coefficient | 0.017 | afterburner_thrust_mult | 1.7 |
| fuel_capacity | 3500.0 | fuel_rate_normal / ab | 1.4 / 9.0 |
| radar_range | **7000 px（= 14 km）** | radar_half_angle | 35.0° |
| lock_time | 2.0 s | combat | `default_combat.tres` |
| icon_color | (0.6, 0.4, 0.7, 1) 紫 | wing_color | (0.3, 0.2, 0.4, 1) 暗紫 |
| equipment | `[enemy_railgun.tres]` | team | 敌方（由 spawner 注入） |

> 🎯 **设计耦合**：`radar_range` 7000 px = 14 km，**刻意等于电磁炮 `max_range_m` 14000 m**——
> 雷达视距与射程对齐，AF-03 看多远就能打多远。改其一必须同步另一。

### 2.2 武器：电磁炮（`enemy_railgun.tres`，RailgunEquipment）

| 字段 | 值 | 说明 |
|---|---|---|
| damage | 60.0 | 单发；**不随等级缩放** |
| max_range_m | 14000 m | 最大射程 |
| min_engage_range_m | 2000 m | 低于此不充能 |
| fire_cone_half_angle_deg | 8.0° | 目标须在机头 ±8° 锥内 |
| hit_radius_px | 12.0 px（≈24 m 束宽） | hitscan 命中半径 |
| charge_duration | **2.0 s** | 扇形从 30° 半角收缩到 0° 的预警时长（脚本默认 2.5，.tres 覆写为 2.0） |
| lock_trajectory_at | **AT_FIRE_TIME（值 1）** | 见 §3.1 —— 预测提前量，非"充能即锁死" |
| fire_delay_after_lock | 0.5 s | 充能完成到开火的延迟，用于预测提前量 |
| cooldown | 9.0 s | 单发后冷却 |
| base_miss_chance | 0.15 | 每发 roll 的基础失手 |
| cloud_miss_bonus | 0.30 | 目标在云中追加（按云密度线性插值） |
| low_alt_miss_bonus | 0.20 | 目标低空追加（雷达地杂波 + 建筑遮挡） |
| low_alt_threshold_m | 3500 m | AltitudeTier.LOW 阈值 |
| fast_target_miss_speed_kmh | 1500 | 高速散布阈值 |
| fast_target_max_miss_chance | **0.0** | AF-03 关闭高速散布（恒定零） |
| beam_fade_duration | 1.0 s | 敌人束消隐（玩家版 0.25s） |
| enemy_beam_color | (1.0, 0.5, 0.4, 1) 红橙 | 敌人束颜色（玩家版蓝白） |

**命中率叠加**（每发开火时单次 roll，§3.2）：
- 晴空常规高度：15%
- 满云：15% + 30% = **45%**（封顶）
- 低空满云：15% + 30% + 20% = **65%**（最高）

### 2.3 出生 / 名册（`survivor_data.gd`）

| 字段 | 值 |
|---|---|
| EnemyType | AF03（enum 17） |
| 解锁等级 | **7**（2026-07-28：原 8） |
| 出现概率/级 | **+6%**（`clamp((level-7+1)×0.06, 0, 0.26)`；原 +5% / 起算等级 8） |
| 概率封顶 | **26%**（Lv11+ 达到；原 18%） |
| **战区池** | **在编**（2026-07-28 新增）：战区任务敌人表中权重 **0.5**、unlock **7**、peak **10**、**不淘汰** |
| Token 成本 | 7 |
| 实例上限 | **1**（同时只 1 架，稀有精英身份不变） |
| 生成形式 | 单机（不编队） |
| 作战高度档 | **偏高空**（见 §2.3.1；原为均匀随机 LOW/MID/HIGH） |
| `_pick_enemy_type` 优先级 | 紧跟 MiG-31 之后、Su-35/Su-27 之前判定 |

概率曲线（新）：Lv7=6% · Lv8=12% · Lv9=18% · Lv10=24% · Lv11+=26%。
（旧曲线：Lv8=5% · Lv9=10% · Lv10=15% · Lv11+=18%）

**为什么调（2026-07-28）**：旧配置**实测一整局遇不到一次**——AF-03 只挂在旅途随机池、
且要先被 MiG-31 等更高优先级机型让位，18% 上限落到实际抽取里稀释得几乎归零，
"电磁炮试验机"这个内容形同不存在。三处一起调（解锁提前 1 级 / 每级斜率 +1% / 上限 18%→26%）
再加**进战区池**（战区任务里也可能碰到它），把它拉回"偶尔遇到、遇到就是一场硬仗"的稀有精英位；
实例上限仍锁 1，保证"稀有"不变成"成群"。

#### 2.3.1 作战高度档（2026-07-28 新增）

此前**全机型共用均匀 1/3 随机档**（LOW/MID/HIGH），AF-03 有 1/3 概率刷在低空——
而它自己的电磁炮对低空目标有 +20% 失手加成、自身低空也自缚射界，等于三分之一的场次
它在跟地形和杂波打架而不是跟玩家打架。

| 项 | 值 |
|---|---|
| 档位偏好 | **偏高空**（HIGH 权重最高，MID 次之，LOW 最低） |
| 理由 | **取射界**——高空视野开阔，14 km 射程与 ±8° 火控锥都需要一条干净的直线弹道 |
| 巡逻高度 | 随抽到的档位取值（HIGH 档 8500~11000 m） |

> 档位与巡逻高度**必须同步**：AI 的巡逻高度经态势层决定战术交战高度，只改档位不改巡逻高度
> 的话，分档只会影响巡逻段、一进交战又被拉回原样。全机型分档权重表见
> [survivor-loop §4.4](../systems/survivor-loop.md)。

### 2.4 等级缩放（通用 manned 缩放，对 AF-03 部分生效）

| 缩放项 | 公式 | 对 AF-03 |
|---|---|---|
| HP 乘子 | `1.0 + (lvl-1)×0.15` | 生效（Lv15 ≈ 279 HP） |
| 导弹数追加 | `int(lvl/4)` | 无效（无导弹） |
| 机炮伤害乘子 | `1.0 + (lvl-1)×0.08` | 无效（无机炮） |
| **电磁炮伤害** | — | **恒定 60，不缩放**（设计刻意） |

## 3. 行为与公式（How）

### 3.1 充能 → 锁定 → 开火（AT_FIRE_TIME 模式）

```
进入开火意图（目标在雷达锁定 is_locked、±8° 锥内、2000–14000 m）：
  → 开始充能：扇形 UI 从 30° 半角收缩，charge_duration = 2.0 s
  → 充能中若目标脱锁 / 出锥 / 丢失 → 取消（charge_persistent=false）
充能完成（progress ≥ 1.0）：
  → AT_FIRE_TIME：aim_pos = 目标当前位置 + 目标速度 × fire_delay_after_lock(0.5)
     （预测提前量；直线匀速目标会被精准命中，变速/转向会偏）
  → 等待 fire_delay_after_lock 0.5 s
  → _fire()：见 §3.2
开火后 cooldown 9.0 s 才能再次充能
```

> ⚠ **与"敌人版充能即锁死"的区别**：枚举有两档——`AT_CHARGE_START`（充能瞬间锁死旧位置，
> 玩家可靠简单机动躲掉）与 `AT_FIRE_TIME`（开火时按当前位置 + 速度预测）。AF-03 用
> **AT_FIRE_TIME**，所以"直线飞=被打中"，主要反制是**变速/急转打乱提前量**与**环境失手**（§3.2），
> 不是简单侧移。

### 3.2 开火与命中判定

```
_fire():
  aim_pos = locked_aim_pos
  miss_chance = base_miss_chance(0.15)
  若目标在云中:   miss_chance += cloud_miss_bonus(0.30) × 云密度
  若目标 altitude < 3500 m:  miss_chance += low_alt_miss_bonus(0.20)
  roll: 若命中失败 → 在最终方向加横向角度扰动（打偏，仍发射束）
  发射 hitscan 闪电束：从机口沿 aim_pos 方向延伸到 max_range_m
  穿透：束路径上所有 CombatUnit / Missile / GroundUnit 受 60 伤
        ⚠ 舰船例外：按母舰归并，一发一舰只结算一次（见下）
  视觉：4 段抖动闪电，红橙色，1.0 s 消隐
```

**穿透结算的舰船例外（2026-07-28 修正，敌我两版电磁炮共用）**：舰船的**船体**与它的
**挂点 / 弱点代理**同时存在于战斗单位表中，而代理受伤会把伤害**原样转发母舰**——
一条有半径的射线能同时扫到船体 + 多个代理，**一发打出数倍伤害**（这正是"航母被一炮秒"的来源）。

规则：**命中点按所属母舰归并，每艘舰只保留沿弹道最靠前的那一个命中点结算**——
一发电磁炮对同一艘舰只结算一次伤害。飞机 / 地面单位的命中路径不受此规则影响
（它们没有代理转发关系，穿透语义照旧）。

高速散布（仅 `fast_target_*`）AF-03 关闭（max_chance=0），故只有环境 miss 起作用。

### 3.3 AI 节奏（Schemer + Lancer 式打带跑）

| 参数 | 值 | 说明 |
|---|---|---|
| 接战模式 | `bvr_only = true` | 仅超视距 |
| standoff 下限 | `bvr_standoff_min` 2500 px（5 km） | 维持距离 |
| 脱离距离 | `bvr_flee_distance` 4000 px（8 km） | 超近就退 |
| 战术 | `prefer_nose_aligned_weapon = true`（SNIPER_HOLD） | 机头对准型武器战术 |
| evade_missiles | true | 来弹规避 |
| aggression | 0.95 | |
| engage_cooldown | 7.0 s | 脱离后再接战间隔 |
| engage_duration | 10.0 s | 单次接战时长 |
| skill_level / composure / focus | 0.92 / 0.88 / 0.95 | |
| self_preservation | 0.5 | 占优时会坚守 |

节奏：约 10 s 接战 → 一发 → 退到 8 km → 7 s 冷却 → 再逼近，整周期 ≈ 17 s。

## 4. 结构与组成（Structure）

普通 Aircraft（非 boss）：`AircraftParams(enemy_af03.tres)` + 一件 `RailgunEquipment`，挂
`AIController`。无挂点系统、无子控制器。电磁炮逻辑全在 `RailgunEquipment.tick/_fire`（state 走
`equipment_state["railgun"]` 字典，不写实例字段，多 mount 复用同一 .gd 类）。silhouette 用默认
fighter（未覆写）。受 7000 px 远距清理（未设 `skip_far_cleanup`）。

## 5. 验收标准（Acceptance / Litmus）

- [x] 充能扇形恰好 2.0 s 收缩；脱锁/出锥即取消
- [x] AT_FIRE_TIME：直线匀速目标被精准命中；急转/变速可偏掉
- [x] 开火后冷却 9.0 s，期间不再充能
- [x] 2000 m 内不充能；超 14000 m 不接战；±8° 锥外不开火
- [x] 命中 roll：晴空 15% / 满云 45% / 低空满云 65%；束穿透并摧毁路径上导弹
- [x] 电磁炮伤害恒 60，跨等级不变；HP 按 `90×(1+0.15(L-1))` 缩放
- [x] 同时最多 1 架（instance_cap=1）；单机不编队
- [ ] **出场率（2026-07-28 新配置）**：Lv7 起 6%→26% 概率；战区任务敌人池里也会出现（权重 0.5，Lv7 解锁 / Lv10 峰值 / 不淘汰）；
      一局 10 分钟内**至少遇到一次**（旧配置实测整局零遭遇，是本次调整的验收锚）
- [ ] **高度档偏高空**：连续多次刷出统计，HIGH 档占多数、LOW 档罕见；巡逻高度随档位同步（HIGH 档 8500~11000 m）
- [x] radar_range 与 max_range_m 对齐（7000 px = 14000 m）
- [x] Archetype 名（Schemer）不出现在 UI / display_name（见 feedback_ai_archetype_internal）
- [x] 性能：单机 + hitscan，无每帧全场扫描；Lv8+ 压测 FPS 掉幅 < 15

## 6. 实现计划（Task Pipeline）

> 已落地（status: done）。保留作"从零重建"工单参考。

### 阶段 1 — 资源
- [x] `enemy_af03.tres`（§2.1）
- [x] `enemy_railgun.tres`（§2.2，AT_FIRE_TIME / charge 2.0 / cooldown 9 / 环境 miss）

### 阶段 2 — 接入名册
- [x] `EnemyType.AF03(17)` + `_create_enemy` case
- [x] `survivor_data.gd`：UNLOCK 7 / CHANCE 0.06→0.26 / TOKEN 7 / CAP 1 + 战区池行（权重 0.5）
      + 高度档权重（偏高空）（2026-07-28 改配；初版为 UNLOCK 8 / 0.05→0.18 / 无战区池 / 均匀档）
- [x] `_pick_enemy_type` 优先级（MiG-31 后、Su-35 前）

### 阶段 3 — AI
- [x] AI 配置块（§3.3：bvr_only + SNIPER_HOLD + standoff 2500/4000 + Lancer 节奏 + 等级缩放）

### 阶段 4 — 收尾
- [x] 调试面板下拉项 + i18n
- [x] 同步 enemy-index.md / code-index.md

## 7. 索引锚点（Where —— 指针，会腐烂，非权威）

| 关注点 | 文件 |
|---|---|
| 本体参数 | `resources/enemy_af03.tres` |
| 电磁炮参数 | `resources/enemy_railgun.tres` |
| 电磁炮行为（充能/锁定/开火/miss/穿透） | `scripts/equipment/railgun_equipment.gd` |
| 名册常量（unlock/chance/token/cap） | `scripts/survivor/survivor_data.gd` |
| `_create_enemy` / AI 配置 / `_pick_enemy_type` | `scripts/survivor/survivor_spawner.gd`（旧 survivor_mode.gd） |
| SNIPER_HOLD / nose-aligned 战术 | `scripts/ai/`（bfm_tactics） |
| reference 索引 | enemy-index.md 的 AF03 行 |

## 8. 变更记录

| 日期 | spec_version | 改动 |
|---|---|---|
| 2026-07-28 | 2 | **出场率整治 + 高度分档 + 电磁炮对舰结算修正**：①出场三连调（解锁 8→**7**、每级概率 0.05→**0.06**、上限 0.18→**0.26**）+ **新进战区池**（权重 0.5 / unlock 7 / peak 10 / 不淘汰），实例上限仍为 1——理由是旧配置**实测整局遇不到一次**，"电磁炮试验机"形同不存在；②作战高度档由"均匀随机"改**偏高空**（§2.3.1，取射界；巡逻高度随档同步，否则分档只作用于巡逻段）；③§3.2 补**穿透结算的舰船例外**：按母舰归并、一发一舰只结算一次（船体与挂点/弱点代理会重复吃同一发，是"航母被一炮秒"的来源），飞机/地面命中路径不变 |
| 2026-05-30 | 1 | 从源码逆向回填为 reconstruction-grade spec；修正 lock_trajectory_at 为 AT_FIRE_TIME（预测提前量，非充能即锁死） |

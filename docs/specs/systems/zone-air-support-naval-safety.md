---
id: zone-air-support-naval-safety
kind: system
status: done
schema_version: 1
spec_version: 8
owner: noelu
depends_on: [60km-density-pass, global-awareness-roe]
reconstruction_complete: true
---

# 战区友军空中支援与海上摆位硬闸

> 保留现有对地、对空、对舰战区任务；已购买对应生涯授权时，每局首次合资格对空任务由 2～4 架 F-86、首次合资格非机场对地任务由 2 架 A-10 第三方友军加入战斗，任务结束后幸存者物理撤离地图；对舰任务使用 4/5/6 艘全目标舰队，并在任何地图几何下都不得生成到陆地上。

## 1. 设计意图（Why）

- **体验目标**：不改变现有战区任务池和奖励循环，用对空战斗机与对地攻击机两类临时支援增强多方交战感，并把“舰船上岸”从软警告收紧为不可能生成的硬约束。
- **现有任务分工保持不变**：随机战区继续保留 `ground / air / squadron / naval`；机场继续使用独立 `airfield` 解放任务。本 spec 不删除对地任务、不改任务权重或奖励。v5 按用户实战反馈，只提高 `naval` 的舰船数量与目标数量。
- **友军定位**：友军是“临时参战的第三方小队”，不是玩家僚机。它们能分担火力和制造多线交战，但不响应玩家命令、不加入玩家编队、不替玩家产生 XP/功勋。
- **Litmus 自检**：
  - 战场氛围：对空与对地任务从单纯的玩家小队对敌 TGT，变为两支友军力量共同压入战区；友军真实交战、会被击落、会撤离。
  - 信息可察觉：友军使用第三方绿色机体/标签，沿战区边缘物理入场；不靠隐藏数值表达支援。
  - 单杠杆：F-86 规模只由战区星级决定，A-10 固定 2 架；不叠玩家等级、热度或随机倍率。
  - 性能红线：不增加逐帧全场扫描；生命周期只维护每个已触发支援战区自己的 2～4 架引用。
- **反模式规避**：
  - 不给友军无敌或锁血；被敌方击毁即正常减员，且不补刷。
  - 不在玩家镜头内瞬间生成或消失；入场从不可见位置飞入，退场飞出地图后释放。
  - 不让友军击杀给玩家 XP/功勋，保持 `ALLY` 第三方统一契约。
  - 舰队摆位不再“找不到全水解也照刷并警告”；宁可缩圈、减护卫、原地驻泊或退化为空战，也不生成一艘落陆舰船。

## 2. 数据定义（What —— 全部数值，权威源）

### 2.1 任务范围

| mission_type | 本 spec 行为 |
|---|---|
| `ground` | 已购 `support_zone_ground` 时，首次 ACTIVE 生成 2 架 A-10 对地支援 |
| `air` | 已购 `support_zone_air` 时，首次 ACTIVE 生成 F-86 友军支援 |
| `squadron` | 已购 `support_zone_air` 时，首次 ACTIVE 生成 F-86 友军支援 |
| `naval` | 无友军空中支援；启用 §2.4 海上摆位硬闸 |
| `airfield` | 完全不变；继续走机场解放与机场防空伞 |

### 2.2 友军支援规模

| 战区难度 | 支援数量 | 机型 | 编成 |
|---|---:|---|---|
| 1★ | 2 | F-86 | 1 长机 + 1 僚机 |
| 2★ | 3 | F-86 | 1 长机 + 2 僚机 |
| 3★ | 4 | F-86 | 1 长机 + 3 僚机 |
| 任意星级 `ground` | 2 | A-10 | 两机分队，各自搜索地面目标 |

- 对空支援固定使用 **F-86**：复用现有完整战斗机生成/武器/AI 管线；强度低于中后期主力敌机，职责是分担火力和制造战场感，不取代玩家完成任务。
- 对地支援固定使用 **A-10**：复用旧基础飞行参数与 GAU-8，但移除火箭和鱼雷；只允许选择 `GroundUnit`，不得跨区攻击飞机或舰船。巡航高度固定 3200m，地面交战沿用扁平高度语义。
- 每个合资格战区每次激活最多生成 **1 支**支援小队；任务重开后可在下一次激活重新获得一支。
- 两类支援均是生涯商店“战场支援”页的独立永久授权，价格各 **3000 功勋**、恒上架、不可退款。正式局只在对应授权已购时生成；bench / boss debug 等非正式局 fail-open，保持验证基线。
- 支援机阵营统一为 `TEAM_ALLY(2)`，类别保留第三方 `ally`，另写 `zone_support=<zone_id>` 作为生命周期归属。
- 支援机 `token_cost=0`、`skip_far_cleanup=true`；不计敌实例上限，不进入玩家编队，不响应点击/轮盘/数字键。
- 支援机使用 `FactionPalette` 的 `COL_FRIEND_ALLY=#59D16B` 绿色；机翼色使用既有 ALLY 转换值 `(0.22, 0.50, 0.28)`。
- 支援机击杀敌方单位：任务目标照常死亡并计入任务进度，但玩家获得 **0 XP、0 功勋**；kill feed 按第三方友军色正常显示。
- 支援机死亡：正常坠毁，不导致任务失败，不补充新机。

### 2.3 入场、驻留与退场几何

| 字段 | 值 | 说明 |
|---|---:|---|
| 入场候选数 | 8 | 沿战区中心周围等角候选 |
| 入场半径 | `max(2400px, zone.radius × 0.9)` | 从战区外缘附近切入，不从地图边界长途赶路 |
| 候选优先级 | 玩家机头前半球优先，其次全圆 | 遵守事件不从玩家身后无声生成的约定 |
| 可见性 | 候选生成范围与当前镜头不得重叠 | 若 8 个候选均可见，延后 0.5s 重试，不在镜头内 pop-in |
| 初始航向 | 指向战区中心 | 长机物理飞入；僚机按小队阵型展开 |
| 驻留半径 | `max(1200px, zone.radius × 0.48)` | 与对空 TGT 的战区盘旋尺度同源 |
| 撤离目标 | 从当前长机位置最近的地图边界外 1200px | 全队直飞出界 |
| 出界释放距离 | 地图边界外 800px | 静默释放，不走击杀/XP 结算 |
| 撤离检查频率 | 0.5s | 只扫该支援小队的存活引用 |

### 2.4 对舰任务水域硬约束

| 战区难度 | 完整编成 | TGT 数量 | 设计锚点 |
|---|---|---:|---|
| 1★ | 4 FFG | 4 | 对齐 1★ 空战任务的 4 个 TGT；以多舰 SAM/AA 火网替代单旗舰速杀 |
| 2★ | 2 DDG + 3 FFG | 5 | 对齐 2★ 空战任务的 5 个 TGT；双 DDG 提供 VLS、CIWS 与 Flak 交叉火力 |
| 3★ | 1 CG + 2 DDG + 3 FFG | 6 | 对齐 3★ 空战任务的 6 个 TGT；CG 为核心、双 DDG 与三 FFG 构成立体防空 |

- 完整编成的**每一艘舰船都是任务目标**；只有全部存活舰船被击毁，任务才完成。
- 不增加单舰 HP，不改变武器参数；难度只由可见的舰船数量、舰型构成与交叉火力提升。
- 这条全舰 TGT 契约是数量调整的一部分：旧逻辑只登记旗舰，单纯增加非 TGT 护卫无法解决“一波齐射击毁旗舰即结束”。

| 字段 | 值 | 说明 |
|---|---:|---|
| 默认旗舰盘旋半径 | 900px | 保持现状 |
| 盘旋半径降级序列 | 900px → 450px → 0px | 0px 表示原地驻泊 |
| 摆位圆心偏移候选 | 0px；900px 与 1800px 的八方向候选 | 保持现有搜索范围 |
| 轨道复核步长 | **40px** | 运行时与现有加密回归同精度；替代 150px 软采样 |
| 合法条件 | `land_hits == 0` | 旗舰和全部护卫的整个轨道采样均在水面 |

找不到完整编成的全水解时，按以下顺序降级，每一步都重新跑全部盘旋半径与圆心候选：

1. 完整编成，盘旋半径 900 → 450 → 0px；
2. 每轮移除轨道半径最大的 1 艘护卫，再试 900 → 450 → 0px；
3. 只剩旗舰时再试 450 → 0px；
4. 若连单舰原地驻泊也没有 `land_hits == 0` 的位置，则**不实例化任何舰船**，把该战区运行时任务类型改为 `squadron` 并生成原有对空任务。

任何 `land_hits > 0` 的候选都只能用于继续搜索，禁止进入舰船实例化阶段。降级结果写 EventLogger；正常找到完整全水解不弹玩家提示。

## 3. 行为与公式（How）

### 3.1 战区支援生命周期

| 状态 | 进入条件 | 行为 | 退出条件 |
|---|---|---|---|
| `NONE` | 合资格任务已预刷但未激活 | 无支援实体；任务可无限等待玩家 | 战区首次 `mission_triggered` 且拥有对应授权 → `INGRESS_PENDING` |
| `INGRESS_PENDING` | 任务 ACTIVE | 按 §2.3 寻找镜头外候选；不可见候选不足则每 0.5s 重试 | 找到候选并生成整队 → `ON_STATION` |
| `ON_STATION` | 整队已生成 | 飞向战区中心并绕战区盘旋；F-86 只攻击飞机，A-10 只攻击地面单位 | 任务完成、战区取消、BOSS 阶段锁区或 Debug purge → `EGRESS`；全队阵亡 → `ENDED` |
| `EGRESS` | 战区生命周期结束 | 清除当前战斗目标，写撤离航点并开启加力；受到攻击可按既有 EGRESS 自卫语义短暂还击，威胁解除后继续撤离 | 全部幸存者飞到边界外 800px 或阵亡 → `ENDED` |
| `ENDED` | 无存活支援机 | 清理引用，不再补刷；本局对应 `fighter` / `attack` 权益额度保持已消费 | 下一局新建战区任务执行器后恢复额度 |

支援生成发生在任务真正激活之后，故玩家不进入战区时没有友军与敌方提前交战，也不会自行完成任务。

### 3.2 交战与任务结算

```text
air_support_count = {1★: 2, 2★: 3, 3★: 4}
ground_support_count = 2

任务完成判定 = 原有对空 TGT 全灭；对舰为安全方案实际保留的全部舰船全灭
友军击落 TGT = 正常推进任务完成判定
友军全灭 = 任务继续，玩家自行完成
任务结束 = 立即下达撤离，不在镜头内 queue_free
```

- 支援机不标 `TGT`，不加入 `_spawned_zones`；单独由战区支援生命周期表持有。
- 支援机属于正常敌对关系矩阵中的 ALLY，敌方战区中队可感知、锁定和攻击它们。
- ALLY 的绿色已承担阵营辨识，标签只移除旧 `ALLY-` 前缀；缩略的是机型名称后缀，呼号必须完整保留。例如 `F-15 Eagle` + `Falcon-2` 显示为 `F-15 E Falcon-2`，`Hound-1` 不得缩成 `H`。
- F-86 只主动攻击敌方飞机；A-10 只主动攻击 `GroundUnit`。两者都不得攻击舰船，也不得被其他战区目标跨区拽走。
- F-86 与 A-10 两项购买权益分开记每局额度：每项只响应本局首次合资格战区；先触发对地任务不会吞掉制空支援，反之亦然。后续同类战区不再派队。
- 若两种首个合资格战区同时激活，两队仍各守所属锚点，禁止互相重定向。

### 3.3 对舰生成事务

```text
先计算安全编成与摆位
if land_hits == 0:
    一次性实例化旗舰与剩余护卫
else:
    继续缩圈 / 减护卫
if 单舰驻泊仍无全水解:
    mission_type = squadron
    生成对空 TGT
```

- 摆位计算完成前不得 `add_child()` 任何舰船，避免失败候选留下半支舰队。
- 护卫删减只影响该次任务实际编成，不改战区星级或奖励；安全方案最终保留的旗舰与护卫全部登记为 TGT。
- 对舰任务完成时安全方案保留的全舰都已击毁；任务取消、重刷与 BOSS 锁区时，整支目标表继续走既有清理语义。

### 3.4 清理覆盖面

以下入口必须统一触发该战区支援的 EGRESS 或安全清理，不能遗留绿色飞机：

- 正常 `mission_completed`；
- BOSS 解锁时 `cancel_all_zone_missions`；
- 战区 `reset_zone`；
- Debug `purge / force_respawn`；
- 场景退出时静态引用清理。

## 4. 结构与组成（Structure）

| 组成 | 职责 |
|---|---|
| 战区任务执行器 | 在 `air/squadron/ground` 激活时查询对应购买权益并建立支援小队；维护 `zone_id → lifecycle + members`；任务结束时下达撤离 |
| 支援飞机工厂 | F-86 复用既有敌机工厂；A-10 复用基础参数并剥离鱼雷，随后统一转 ALLY |
| ALLY 转换器 | 统一保证 team、颜色、Token、选择与奖励契约 |
| 小队/AI | 复用现有 Squad 编队、战区盘旋航点、目标选择与 EGRESS 自卫语义 |
| 海军摆位器 | 用 40px 轨道步长返回严格全水候选；不负责实例化 |
| 战区海军生成器 | 按 §2.4 降级编成；只在拿到 `land_hits==0` 后事务式创建舰队 |
| 回归测试 | 水域加密复核、无水解硬闸、支援数量/阵营/一次性生成/撤离清理 |

不新增 AutoLoad，不在共享层写生存模式分支，不新增每架飞机的 `_process/_physics_process/_draw` 子节点。

## 5. 验收标准（Acceptance / Litmus）

### 功能

- [x] 对地与机场任务的生成规模、任务权重和奖励保持原样；机场/对舰不生成本 spec 支援。对舰规模与完成条件按 v5 单独增压。
- [x] 正式局未购对应授权时不生成支援；两项授权均恒上架、各 3000 功勋；非正式局 fail-open。
- [x] 1★/2★/3★ 对空任务首次 ACTIVE 后分别出现 2/3/4 架绿色 F-86；非机场对地任务首次 ACTIVE 后出现 2 架绿色 A-10；未 ACTIVE 时不生成、不交战、不自行完成。
- [x] F-86 只攻击飞机；A-10 仅保留 GAU-8、移除火箭/鱼雷且只攻击 GroundUnit；两者均拒绝舰船。
- [x] 支援从镜头外物理飞入，编成一支小队并在所属战区作战；不响应点击、轮盘、数字键。
- [x] 友军会被敌机正常锁定/击毁；全灭不导致任务失败且不补刷。
- [x] 友军击落 TGT 可推进并完成任务，但玩家 XP/功勋不增长，kill feed 使用友军色。
- [x] 任务完成后幸存支援机清目标、加力飞向最近地图边界；画面内不消失，飞出边界 800px 后释放。
- [x] BOSS 锁区、战区 reset、Debug purge/respawn 后均无支援机残留；同一局后续同类任务不会重复生成第二队。
- [x] F-86 / A-10 权益各自每局至多出动一次；两类额度互不吞占，首个合资格任务同时激活时各守所属锚点。

### 海上安全

- [x] 1★/2★/3★ 完整编成分别为 4 FFG、2 DDG+3 FFG、1 CG+2 DDG+3 FFG，对应 4/5/6 个 TGT。
- [x] 击毁旗舰但仍有护卫存活时任务不得完成；安全方案保留的最后一艘舰船击毁后才完成。
- [x] 战区 E 的 1★/2★/3★ 舰队以 40px 轨道步长复核，全部 `land_hits == 0`。
- [x] 人工构造完整编成无解但缩编有解的几何时，先减护卫再生成，任何已生成舰船初始位置均不在陆地。
- [x] 人工构造连单舰驻泊都无解的几何时，零舰船实例残留并自动改为空战任务。
- [x] 运行时禁止出现 `land_hits > 0` 后继续 `_make_zone_ship` 的路径；原软警告不再是放行条件。
- [x] 既有 CSG 南北摆位回归保持全绿，本改动不破坏 BOSS 舰队。

### 工程门

- [x] 自动化：当前合并回归 `meta_shop` 88/88、`zone_air_support` 55/55；既有 `naval_zone_water` 26/26。本批同时覆盖每局一次/新局重置、短标签、Hound 呼号与 3000m 雷达。
- [x] v5 自动化：`naval_zone_water` 26/26（含真实六舰实例化、全舰 TGT、旗舰速杀负例）；`naval_formation` 10/10、`bomber_rotor_airburst` 22/22、`boss_progression` 22/22。
- [x] CIWS 审计 14/14：航母四 CIWS 对 5/8 枚正向齐射均为 100% 拦截；用户裁定其符合航母 BOSS 强度，不再作为平衡红项。
- [x] 性能：零新增逐帧全场扫描；撤离检查 0.5s 且只扫 2～4 个成员引用。
- [x] 性能：Lv15 + Sentinel 完整机群 + 8 F-86 + 2 A-10 三支援压力样本共 49 架，146 headless FPS；与普通压力样本退出警告基线一致，无新增泄漏。
- [x] v5 性能：39 架飞机 + 6 艘 3★ 舰船 + 18 个舰载锁定代理，共 63 单位；`naval_zone_stress` 12 秒样本最后一秒 146 headless FPS，舰载武器约 67µs/帧。
- [x] 已知 seam：`verify_player_ref_holders.py` 通过，漏登记 0。
- [x] 文档：新增函数/入口已同步 script-index 与 code-index，新增强锚点全部通过；全局历史锚点红项见 §8。
- [x] i18n：两项商品名称/说明中英日齐全，Godot 已重导三份 translation 资源。

## 6. 实现计划（Task Pipeline —— 工作令）

### 阶段 1 — 海上摆位硬闸
- [x] 海军摆位轨道采样步长收紧到 40px，并使生成器只接受 `land_hits == 0`。
- [x] 加入缩圈、逐艘减护卫、单旗舰驻泊与最终空战 fallback；舰船改为安全方案确定后事务式创建。
- [x] 扩展海上水域回归，覆盖缩编与零舰船残留。

### 阶段 2 — 对空战区支援
- [x] 增加每战区支援生命周期状态与 2/3/4 数量映射。
- [x] 复用 F-86 生成 + ALLY 转换 + Squad 编队，接入镜头外入场与战区驻留。
- [x] 接入正常完成、BOSS 锁区、reset、Debug 清理和物理撤离。
- [x] 增加生命周期/阵营/奖励隔离/多战区回归。

### 阶段 3 — 验收与索引
- [x] 跑相关 bench、玩家引用校验、文档新增锚点校验。
- [x] 跑 Sentinel + Lv5+ 双支援压力测试（覆盖单支援上界）。
- [x] 填 §7 代码锚点、同步 reference 索引、记录 §8 落地结果并转 `done`。

### 阶段 4 — 对地支援与生涯购买门槛
- [x] MetaShop 增加 `support_zone_air` / `support_zone_ground`，各 3000 功勋且恒上架。
- [x] `ground` ACTIVE 后生成固定 2 架 A-10；复用统一入场、驻留、收益隔离与撤离生命周期。
- [x] A-10 移除火箭与鱼雷，并在 AI 选敌入口锁为只攻 GroundUnit。
- [x] 更新商店三语、定向测试、压力样本与 reference 索引。
- [x] 定向 bench / 性能样本 / 玩家引用校验通过；新增 spec 锚点通过，reference 全局仍有本批之外的既有陈旧锚点。

### 阶段 5 — 海上任务编成增压
- [x] 把完整舰队提升为 4/5/6 艘，并用单一编成表驱动实例化与回归。
- [x] 把安全方案实际保留的所有舰船登记为 TGT，移除“只杀旗舰即完成”的捷径。
- [x] 扩展 `naval_zone_water`，覆盖三档舰型构成、4/5/6 目标数、缩编后全员 TGT 与水域占地。
- [x] 跑海上水域、舰队运动、Flak、CIWS 审计与压力回归；新增交叉火力不突破 60 FPS，航母 BOSS CIWS 强度按用户裁定保持。

## 7. 索引锚点（Where —— 落地后填）

| 关注点 | 文件 |
|---|---|
| 战区任务与支援生命周期 | `scripts/survivor/zone_mission.gd` |
| 第三方飞机转换 | `scripts/events/ally_force.gd` |
| 舰队水域校验 | `scripts/naval/naval_placement.gd` |
| 对舰水域回归 | `scripts/tests/test_naval_zone_water.gd` |
| 支援生命周期回归 | `scripts/tests/` 下新增对应测试 |
| reference 索引行 | `docs/reference/script-index.md` · `docs/reference/code-index.md` |

## 8. 变更记录

| 日期 | spec_version | 改动 |
|---|---:|---|
| 2026-08-09 | 8 | 修正当前操控机基础显示名已含昵称时的漏网：`F-14 Tomcat [Ultra]` 现在先提取真正机型编号，固定显示为 `F-14 [Ultra]`；完整运行时名称仍保留给日志等非战场标签消费者。Shadow `presentation` 定向回归覆盖该真实输入。 |
| 2026-08-08 | 7 | 用户澄清 v6 标签语义：缩略的是 `Eagle` / `Super Flanker` 等机型名称后缀（整段只留一个首字母），不是呼号；`Ultra`、`Falcon-2`、`Hound-1` 均完整保留。示例：`F-14 [Ultra]`、`Su-35 S [呼号]`、`F-15 E Hound-1`。Shadow `presentation` 149/149、`zone_air_support` 55/55。 |
| 2026-08-08 | 6 | 战区 F-86 与 A-10 支援改为每局首次合资格任务各派一次；本版曾把“缩机名”误实现为“缩呼号”，由 v7 纠正。Shadow `zone_air_support` 55/55、`meta_shop` 88/88。 |
| 2026-08-01 | 1 | 用户定向：保留现有对地任务；对舰任务不得上岸；对空任务 ACTIVE 后生成 2～4 架友军支援，任务结束后撤离地图。补全首版数值、生命周期与水域失败降级契约；用户确认后转入实现。 |
| 2026-08-01 | 2 | 实现完成：对舰 40px 水域硬闸 + 缩编/零舰 fallback；对空 2/3/4 架 F-86 支援、只对空、第三方收益隔离与物理撤离。`naval_zone_water` 18/18、`zone_air_support` 25/25；47 架（含 Sentinel + 双 4 机支援）压力样本 146 FPS。全量 `--bench=all` 仍被工作树既有 `career_archive` 持久化与 `spawn_pool` BOSS cost 断言拦截；全局文档校验仍有 script-index 6 / code-index 128 个本批之外的陈旧锚点，本批新增强锚点无红项。 |
| 2026-08-01 | 3 | 用户追加：非机场清剿任务获得固定 2 架 A-10 对地支援；F-86/A-10 两类支援改为生涯商店独立永久授权，各 3000 功勋。`meta_shop` 76/76、`zone_air_support` 35/35、`naval_zone_water` 18/18；49 架三支援压力样本 146 FPS。30 秒首次压力进程曾触发 bench 120 秒回收，随后 3 秒与 10 秒样本均正常退出；普通压力样本具有相同既有退出警告，确认无 A-10 新增泄漏。 |
| 2026-08-01 | 4 | 用户明确 A-10 不得默认携带火箭：战区支援 A-10 改为纯 GAU-8 对地攻击，旧 A-10 基础与实验变体同步剥离火箭。 |
| 2026-08-04 | 5 | 用户反馈对舰任务一轮导弹齐射即可结束，批准增加舰船数量以对齐其它战区任务难度。已落地 1★/2★/3★ 为 4/5/6 艘且全舰 TGT；单舰 HP 与武器参数不变，水域不足时继续按既有硬闸逐艘缩编。`naval_zone_water` 26/26；63 单位压力样本 146 FPS。 |

---
id: combat-entity-volume-prototype
kind: system
status: approved
schema_version: 1
spec_version: 4
owner: AGL
depends_on: [systems/aircraft-top-view-silhouettes, systems/battlefield-visual-scale, systems/inrun-weapon-inventory, systems/ui-design-guidelines, systems/runtime-validation-workflow]
reconstruction_complete: false
---

# 战斗实体立体质感原型

> 游戏仍是俯视 2D，但飞机、实际挂载和战斗实体看起来有体积；先在真实混战中比较效果与成本，再决定渲染路线和铺量。

## 1. 设计意图（Why）

### 1.1 已确认的产品边界

- 保留现有 2D 模拟、俯视操作、Camera2D 输入、虚拟高度、航空物理、AI、命中与伤害。用户于 2026-09-05 追加批准地图三维显示实验：仅实验路径用 Camera3D 正交投影呈现真实网格，不代表全面迁移获批。
- 希望会动的东西、与玩家战斗有关的东西具有协调的立体质感，不只升级玩家飞机。具体类别见 §2.2。
- 参考用户提供的 Tacview 截图：简洁纯色实体、可辨认的机体结构、明暗和遮挡。参考其表现语言，不复制其相机视角、地形系统或软件资产。
- 挂载必须反映真实装备，允许不完全符合现实的挂架、组合模块和机腹武器。美术适配不能新增装备数量限制，也不能破坏已有装备互斥。
- 大规模混战和 60 FPS 优先于模型细节。允许普通单位按屏幕尺寸简化，但不减真实交战、单位、火力或必要反馈。
- 用户先看正常游戏视角中的实际效果，再决定是否推广。单独模型转台、漂亮截图和 headless 通过都不能代替这个决定。

用户于 2026-09-05 亲自试玩后明确满意，并批准飞机专属三维模型与地图并行制作。本文件的 `approved` 现包括该表现路线的分批内容制作，**不表示长时性能过门或正式默认全量迁移获批**。未完成资产与性能验收仍保持 `reconstruction_complete: false`。

### 1.2 设计自检与不做事项

继承 [设计哲学](../../DESIGN_PHILOSOPHY.md) 的信息察觉、武器抽象、60 FPS、变强可见性要求：立体结构应让姿态和装备更容易看懂，不能只在极近截图里有区别；不引入现实挂点兼容性作为新玩法门槛；不改变自动开火节奏。

此次不做正式地图全面迁移、正式自由旋转相机、真实高度空间碰撞、新飞行模型、新武器、装备平衡、全局光照、写实涂装、铆钉和面板线生产。隔离模型实验室允许倾斜观察几何体，但不能用它证明正式点击/碰撞或混战性能。拖尾、激光、曳光、爆炸和 HUD 不要求全部换成三维实体。

### 1.3 与现有权威的关系

- [飞机顶视轮廓](aircraft-top-view-silhouettes.md) 继续约束默认正式路径，包括机型辨识、原始轮廓、机体配色与现有二维绘制。它的“不引入 3D 节点”不被本文件全局删除：**只有显式启用本原型的实验路径**允许比较不同后端。
- [战场视觉尺度](battlefield-visual-scale.md) 继续拥有尺寸、世界单位和高度缩放规则；原型不通过放大全体飞机或改变地图尺度制造效果。
- [局内武器库](inrun-weapon-inventory.md) 与各武器 spec 继续拥有装备合法性、归属、弹药和冷却；本文件只增加只读表现契约。
- [UI 设计规范](ui-design-guidelines.md) 继续拥有标签与提示的通用规则，本文件不建立第二份 UI 规范。
- 若后续选型需要改变上述正式边界，先修订对应 spec 并记录用户裁决，不能以原型通过自动替代原有合同。

## 2. 数据定义（What）

### 2.1 固定约束与待测参数

| 项目 | 本原型规定 |
|---|---|
| 默认启用 | 否；关闭原型时沿用正式渲染路径 |
| 玩法参数变化 | 0；不改速度、G、射程、伤害、发射许可、弹药、补弹时间或装备合法性 |
| 世界与高度 | 沿用现有权威；局部模型厚度不等于新增物理高度 |
| 机体配色 | 沿用机型的 `icon_color` / `wing_color`；不能因换帅、切控或阵营变化把机身自动重染成 UI 阵营色 |
| 敌我识别 | 沿用现有阵营标记、锁定和状态栏；模型着色不取代战术识别 |
| 稳态性能底线 | `frames_below_60 == 0`，`p1_fps >= 60`，`worst_frame_fps >= 60` |
| 同条件三次中位回退上限 | `avg_fps` 不超过 5%；`p1_fps` 不超过 10%；继承性能守则，不放宽门槛 |
| 普通单位视觉档位 | 近景完整体积、中景简化体积、极远景清晰轮廓；具体像素阈值和迟滞待测 |
| 关键单位 | 玩家、BOSS、Sentinel 保留既有豁免；不能因拥挤度降级。屏幕很小时仍须保持身份和关键反馈，不强行堆不可见细节 |
| 资产预算 | 三角面数、材质数、图集尺寸、角度采样、缓存容量和上传预算均待测，不能先写成已验证常量 |
| 实时渲染预算 | 视口数量、分辨率、合成面积、实例数和更新频率均待测；禁止未经测量的一实体一独立视口铺量 |
| 预估工期 | 尚不报全量承诺；原型记录实际制作、接线、适配与验证耗时，再估算扩展成本 |

待测字段必须在候选方案记录中写出实际取值、测量场景和结果；选型后回填可重建的参数。不能用“低多边形”替代明确的提交、显存和 CPU/GPU 成本记录。

### 2.2 目标覆盖与首批样本

下面是原型取样计划，不是所有同类资产已经覆盖的声明。工程取样可在同类中替换，但必须保留对应验证目的并记录实际对象。

| 类别 | 首批取样 | 必须证明 | 后续推广边界 |
|---|---|---|---|
| 固定翼 | F-14、A-10 两种不同结构 | 轮廓、机身/机翼厚度差、座舱/发动机/垂尾、滚转；轻装与多装备均可读 | 其它现实机、原创机和可变结构机逐类验收，不用一架通过代表全部 |
| 旋翼机 / 无人机 | 一架现有直升机与一架无人机 | 旋翼、悬停/平移姿态、很小实体的远景辨识 | 复用实际移动和战斗状态，不能套固定翼假动画 |
| 航空挂载 | 主/副导弹、ESM 吊舱、电磁炮；补一组现有火箭/炸弹配置 | 实际装备映射、槽位共存、离架、空架与补弹恢复 | 盘点正式装备数据源；无法同装的装备分组覆盖，不伪造非法“全武器同时装” |
| 飞行武器实体 | 导弹、火箭、炸弹 | 离架衔接、方向、实体体积、轨迹与命中反馈一致 | 高频细小子弹优先保留批量曳光，不要求逐发模型 |
| 地面战斗单位 | SAM、火炮或炮塔、雷达 | 底座与上部结构、真实炮口方向、损毁与锁定点 | 包含与战斗有关的固定目标，不能因为“不移动”漏掉 |
| 海上战斗单位 | 一艘现有 DDG 及其挂点 | 舰体/上层建筑、独立炮塔、VLS 发射与挂点损毁 | 航母、潜艇、其它舰型分别适配；不把舰船可伤害挂点降成装饰 |
| BOSS / 超级单位 | **Mother Goose 与普通飞机同批实验**：飞翼本体、8 螺旋桨、2 VLS、舰桥；其它 BOSS 建立后续覆盖矩阵 | 特殊轮廓、真实挂点位置/损毁、Adds、阶段效果及关键单位豁免 | 不用普通飞机通过替代 BOSS 验证；列车/陆舰/海军/多机 BOSS 各有适配与实战验收 |

本版增加静态地图候选：读取沙漠 MapDocument 的建筑、道路和铁路生成真实网格；这是显示实验，**不将沙漠地理写入原场景玩法**。实验地图与旧正式底图性能不可直接称为同条件 A/B；必须增加同一三维地图下旧机体/新机体的成对测试。天气、反馈与 HUD 不因实验删除。

### 2.2.1 实时三维候选 A：本次有限实验

- 单个共享 3D 世界和一台正交 Camera3D；无逐机 SubViewport，无新物理服务器、碰撞体或 AI。
- 世界映射为 `(x, visual_height, y)`，1 个 3D 单位 = 1 个现有世界像素。高度仅决定显示层次，不作真实飞行高度；正俯视中不移动点击中心。
- Camera3D 从真实 viewport canvas transform 反算中心、旋转和正交尺寸；逐帧记录投影中心误差，验收最大误差不超过 0.1 屏幕像素。缩放、抖动、旋转保持同源。
- 技术低模由可复建几何代码生成：有分层厚度、机身曲面/折面、薄翼、座舱、尾翼。属于技术代理，不声称逐机型美术完成，也不声称已完成 Blender 生产流水线。
- 静态网格只构建一次；共用顶点色材质与定向光，无 GI、后处理和局部多灯。阴影单独开关，需记录开关状态。
- 普通机体按结构族共用网格、MultiMesh 批提交；只读一次缓存实体列表，0.1 秒刷新成员，逐帧只同步现有变换；离屏普通机不提交。玩家/BOSS不因拥挤或数量丢失。
- Mother Goose 独立机体；部件位置消费 `WeaponMount.world_position()`，消失消费 `destroyed`。不把老图里装饰发动机数量当成可伤害部件数量，不新增随机伤害或假冷却。
- 光学隐形、传感器渐隐、销毁残骸及未适配特殊机型必须安全回退原显示并计数；不能泄露原本不可见的单位。
- 此次实测的第一切片不冒充已完成所有航空挂载/弹药恢复、地面舰船模型和全 BOSS。上述仍为 §6 后续工作，未覆盖要在报告中明确列出。
- 实验键：`volume_3d_c1/c2/mother_goose` 与 `volume_2d_c1/c2/mother_goose`；后者同地图、光照与相机，但保留二维机体。`volume_3d_lab` 只作静态几何/斜视检查，不计混战通过。

本次可复建参数：5 个共享网格族（固定翼代理、A-10 结构代理、Mother Goose、本体之外的螺旋桨、VLS），合计 1200 个**源网格**三角形；地图静态合并网格 17620 三角形。每批预留 128 实例，溢出显式报错而非丢单位；尚不承诺此预算支持正式全量。材质为顶点 sRGB 色、背面剔除、粗糙度 0.82、无高光、无透明；统一环境光能量 0.35、定向光 0.8，正交阴影。实时显示高度暂固定为 600 世界像素，仅用于层次/投影；真实高度仍只消费原缩放，尚未建立跨高度三维遮挡语义。相机高度 50000、near 0.1、far 100000，阴影距离 80000，机体投影跟随真实 Camera2D；Mother Goose 成对样本固定近景缩放 1.2 并跟随母机。独立 lab 使用近景观察光照，不冒充同条件性能对照。

首批仅替换不透明固定翼代理与 Mother Goose；旋翼、其它专属 BOSS、渐隐/隐身、残骸和离屏普通机回退。多机型代理不是机型美术完成；机翼第二色、特殊机动缩放/俯仰、分层高度、航空挂载与完整损毁资产仍待适配。只允许据实际提交数评价这一有限切片。

#### 批次 A 对初版预算的覆盖（v4）

上段 5 批次/1200 面/17620 面为初版留档。当前实验候选为 7 批次、源机体合计 2180 面，地图 3553 面；F-14/A-10/F-47 分别为 432/412/416 面。仅在预热构建并缓存，`Catalog.key_for` 映射专属机型，其余固定翼保持初版代理；不是所有战斗单位已经专属建模。旧 `volume_3d_baseline_c1/c2/mother_goose` 使用初版三维模型与初版三维地图，是批次对照，不是纯二维正式基线。

地图按文档的 `world_size_m × PIXELS_PER_METER` 生成地表；保留界外列车进场段。仅支持本批 union 地理环，未实现 even-odd 孔洞时拒绝构建。非 land 区域色以地表色向源颜色混合 0.35，作为低对比覆盖；无 DEM 时不虚构山高。道路宽度按 motorway/trunk/primary/secondary/tertiary/residential/service 为 25/20/16/12/9/7/6 px，另加 5 px casing；铁路 18 px 道床、10 px 双轨间距、轨宽 1.6 px，24 px 累计弧长枕木，共享接点斜接长度不超过两倍带宽。机场完整继承源多边形，标线沿其长轴；建筑四边形采用低矮双坡顶，最高处保持源高度，其余保留平顶与窄边。只装饰显示，不生成新碰撞。

观察场新增 `5/6` 三机顶视/斜视、`7` 机场、`8` 全图；导出三机与地图 GLB 及来源许可 JSON。A-10 继承现有参考的 Kaboldy / CC BY-SA 3.0 署名与许可；F-47 明确原创概念。地图最大 12000 面的批次预算不等于性能验收通过。

### 2.2.2 用户可操作的实验战斗入口

2026-09-05 用户追加要求亲自在实验场景战斗。本次只补可达入口，不改上一轮渲染预算与性能裁决：

- `scenes/tests/volume_3d_combat.tscn` 可在编辑器按 F6；模型观察场按 `B` 进入同一场景。
- 直接复用正式 Survivor 的 Mother Goose Boss Debug，F-47 参考编队、等级与随机合法 build 由原 Boss Debug 生成；不复制战斗控制器，不启用自动 bench AI、自动选卡或到时退出。
- 默认相机由原 CameraController 持有，可点击下令、滚轮缩放、空格跟随、Tab 查看构筑、Esc 暂停；不执行性能 bench 的强制 Mother Goose 近景跟随。F8 复用原 Boss Debug 重开，重开后仍挂接三维实验层。
- 手动入口的只读显示层在暂停演出期间继续同步导演相机/演员，不解除模拟暂停；舞台对单位 `modulate.a` 的淡出必须同样触发旧显示回退，不能由三维层泄露被隐藏的演员。
- 继续按 Boss Debug 排除生涯入档；模型样区只用于显示，不承诺正式地图碰撞已迁移。未知机型和隐形状态仍按 §2.2.1 回退。
- 自动 smoke 必须清除 wrapper 的 bench 路由后进入与 F6 相同的非 bench 玩法，验证实际命令、镜头、暂停/Tab、重开及三维接线；自动退出仅存在于显式 smoke 调用中。
- 本次主要是一次性入口接线与按键事件，不新增逐实体节点或渲染批次；手动场景复用现有显示循环并允许暂停期运行，另补一个舞台透明度判断。跑 focused、实际 Visual smoke、C1 三维候选复查和默认 all；未改变全场扫描/LOD，豁免重复 C2。用户手动试玩与这次复查不构成性能优化或 60 FPS 毕业，上一轮长测失败保留。

### 2.3 表现数据合同

下列是拟建立的逻辑数据，不表示这些新接口已经实现。表现层只消费权威状态，不获得写回权限。

| 数据 | 所有者 / 内容 | 更新条件 |
|---|---|---|
| 实体身份 | 正式单位实例、机型/部件身份、销毁代际 | 出生、换型、销毁；不得只以可复用的池索引识别 |
| 姿态快照 | 正式位置、航向、坡度及已有机动表现状态 | 随正式变换更新；只影响渲染 |
| 装备快照 | 本机实际已装装备、主/副通道、现有合法组合 | 正式获得、卸载、换型、继承完成后 |
| 挂载布局 | 稳定装备 ID → 机体局部锚点 / 组合架 / 内置模块 | 装备或机型变化时重算，不逐帧重排 |
| 弹药快照 | 通道库存、容量、装填状态及正式发射/补给结果 | 权威状态提交后；不自行倒计时 |
| 可见性快照 | 现有隐形、阵营、选中、损毁和屏幕可见度 | 对应正式状态变化 |
| 资产记录 | 来源、授权、机型映射、坐标约定、布局边界、候选后端与成本 | 离线制作或配置变化；运行时只读取 |

## 3. 行为与公式（How）

### 3.1 局部立体表现，不迁移战斗空间

1. 单位屏幕中心仍由现有世界位置与 Camera2D 决定。新增机体厚度、机翼/尾翼遮挡和明暗发生在局部表现内；虚拟高度继续消费现有尺度合同，不另加把飞机“抬离点击位置”的偏移。
2. 航向、坡度和已有机动姿态来自正式状态。若候选需要新增俯仰推导，只能由已有运动量生成只读视觉值，并先写清公式及限幅；不能反向驱动物理。
3. 机身、座舱、发动机要有起伏，机翼相对薄，垂尾在大坡度下仍有结构。不能只把整张轮廓挤成等厚饼干。
4. 光照用于交代体积，允许简洁平滑面或少量硬边，不强制做成夸张三角切面。转向和滚转时不能明显变成带着固定高光旋转的贴纸；预渲染候选也要接受此检查。
5. 保留正确机型轮廓与主要比例；主机体不能为塞武器而任意拉宽、变厚。细节在正常视距不可见时先删细节，不扩大整个游戏尺度。
6. 机体、挂载、发射闪光、在飞弹体和尾迹使用一致的锚点约定。原型不改物理发射点；可做短暂纯视觉离架衔接，但不能推迟真实出弹、改弹道或造成重复命中。衔接时长、偏移和恢复函数在候选实现前登记。
7. 阴影不作为第一阶段必要项；机体自遮挡与体积明暗优先。若增加地图投影，必须单列成本与视觉高度语义，不能把数千米高的飞机画成贴地物件。

### 3.2 多装备与稳定挂点

- 装备是否合法，完全继承正式武器库与各武器规则。现有“库中装备自动挂载、不新增总量上限”的语义不变；已有互斥组合仍互斥。
- 为每个装备类型定义稳定的视觉归属。优先使用机翼、机腹和机身附属位置；允许多联挂架、组合吊舱、保形模块、内置武器舱和虚构中央电磁炮。
- 同时存在的装备不能分配到同一个互斥几何空间。ESM 等持续设备独占其展示区域，不能被导弹补弹时覆盖。
- 同型重复部件使用固定顺序及子槽 ID；存量增加使用该装备预留的挂架扩展区域，不挪用其它装备的区域。换装备或换机型才允许整体重新适配。
- 大容量武器可采用有内部储弹语义的组合挂架：外部可见的是实际挂架/就绪弹，不承诺把所有库存逐枚悬挂在机翼上。每个装备的可见数量映射必须显式记录；不能无说明地截断装备清单。HUD 库存仍是数量权威。
- 炮口、吊舱和弹体可有合理局部遮挡，但整件装备不能因布局冲突而完全消失。正常距离看出构筑差别，近景可定位装备；极远景无需辨认每枚弹。
- 布局失败时给出可定位的诊断，保留该装备真实功能，并标为未适配；禁止偷偷卸装、停火或把候选回退伪报为已完成。
- 特殊装备跟随正式归属，不因切控而复制到整支编队；换帅/进化后的外观读取迁移完成的实际装备状态。

### 3.3 发射、空架与补弹

必须区分**两次发射之间的冷却**和**库存补充/整仓装填**。视觉层不拥有“时间到了就再长一枚”的第二套规则。

| 正式状态 / 事件 | 外观行为 | 禁止行为 |
|---|---|---|
| 已装备且有就绪弹 | 在该装备预留位置显示对应就绪弹 | 凭武器类型固定画满，与实际库存无关 |
| 正式成功发射并消耗库存 | 对应弹体离架，库存映射刷新；挂架与槽位归属保留 | 把空槽交给 ESM / 其它武器，或重排整机挂载 |
| 仅发射间隔 CD 结束 | 根据当前真实库存更新就绪表现 | 当成补弹事件，无条件恢复已耗尽的导弹 |
| 库存为零，正在装填 | 保留空架 / 发射器本体；持续设备照常存在 | 把整个吊舱、炮塔或机翼武器模块一起删除 |
| 正式补弹、补给或容量升级提交 | 按提交后的库存映射，在原预留位置恢复 | 自行增加弹药；按旧容量或旧计时恢复 |
| 无耗弹/无限弹药下成功发射 | 保留真实离架反馈，下一就绪外观按仍有效的库存规则恢复 | 以“库存没减”判定没有开火；用外观动画阻止下次真实发射 |
| 暂停、JAM 或其它正式计时影响 | 只反映权威进度，不另启普通墙钟计时 | 游戏暂停或装填被阻止时，外观自行补满 |
| 切换 LOD、离屏回屏、关闭再开原型 | 从当前权威库存重建 | 当成新生成，默认满弹 |

例如容量为 2 且逐发耗弹的普通案例：`2 → 发射 → 1 → 发射 → 0 → 正式装填完成 → 2`。这是状态示例，不是给全部武器设定容量 2，也不是把发射 CD 设为装填时间。

主、副通道分别绑定自己的真实库存与装填状态；正式主弹装填若同时补充副弹，表现同步接受该结果，不能为了动画整齐把两条计时器强行合并。齐射、免费窗口、补给、被动发射和特殊武器入口必须逐条接入/审计，不能假设只有一个发射调用点。

### 3.4 视觉 LOD 与关键反馈

- 普通单位按实际屏幕占用尺寸选择完整体积、简化体积或轮廓；阈值以正常开局、总览、近景三种实测尺寸校准，不能只按世界距离。
- 切档设置迟滞并保持中心、方向、颜色和主要轮廓连续。不得出现跳位、尺寸突变、导弹重新长出、尾迹断头或模型与图标重影。具体混合方法及阈值在候选记录中定稿。
- 玩家、BOSS、Sentinel 不进入普通拥挤度降级；对玩家有直接威胁的弹体、被选中/锁定单位的必要辨识也必须保留。
- 仅简化不可辨认的几何与表现计算；不以模型不可见为由停物理、少发导弹、少刷敌人或删火力反馈。现有世界模拟/激活边界不在本轮改写。
- 机体可能使用低成本远景轮廓，HUD、危险提示、目标线、弹道和爆炸仍按各自正式规则绘制；这些不能随模型 LOD 一起关闭。

### 3.5 终态与缓存

出生、装备变化、进化、切控/换帅、阵营转换、隐形/现形、部件损毁、实体死亡、回收复用与场景退出均有明确刷新/释放路径。销毁先使表现绑定失效，再释放占用；下一帧及下次缓存消费不得访问旧对象。

跨帧 Object 读取遵守 `Variant → TYPE_OBJECT → is_instance_valid → is/as/字段`；不能把已释放对象先传进强类型形参，再指望函数内部守卫。新玩家引用持有者必须加入正式切控重定向入口。共用资产可缓存，单位状态、槽位占用和临时渲染目标不能跨实体串用。

### 3.6 UI 与战斗可读性

世界标签继续由统一屏幕空间覆盖层持有，不挂到会滚转的三维模型或局部贴图下面。缩放、窗口尺寸、镜头旋转和单位滚转不得让文字倾斜。模型只提供实际视觉边界，不复制标签绘制规则。

保留现有锁定、选中、危险区、轨迹和受击反馈。目标线的世界端点不能烘进会随机体转动的旧缓存。机身明暗不能遮蔽敌我识别，隐形也必须同时约束新增模型、挂载和多余通道，不能从遗漏的部件泄露目标。

## 4. 结构与组成（Structure）

### 4.1 共用边界

拟采用“正式状态 → 只读表现快照与挂点布局 → 可替换绘制后端 → 现有世界/HUD 合成”的边界。只实现原型需要的最小接口，不提前建设通用 3D 引擎。

布局按装备事件重建；位置和姿态按可见连续性需要更新；资源离线制作或受控预热。不能逐实体逐帧扫描场景树、创建材质、读取图片、生成网格或重新布局。缓存、批绘和临时目标都有可测的容量及释放策略，视觉随机独立于战斗 RNG。

### 4.2 候选路线

| 候选 | 保留 2D 的方式 | 要用原型回答的问题 |
|---|---|---|
| 实时低模合成 | 正式单位仍为 2D，只把局部 3D 模型输出合成到原位置 | GL Compatibility 下的批处理、渲染目标数、透明边缘、姿态、遮挡与合成成本 |
| 离线模型渲染图集 | Blender 等工具生成多姿态素材，游戏读取图集 | 航向/滚转采样和插值是否足够平滑；光照是否像贴纸；动态挂载如何一致遮挡；图集与显存是否可控 |
| 混合 | 近景/关键对象与普通远景使用不同表示，共享权威快照 | 切换是否连续；是否值得承担两套资源与测试维护成本 |

候选必须至少比较“姿态与挂载最灵活的方案”和“预计运行成本更低的方案”。若其中一路因可复现的兼容性或成本问题提前淘汰，记录取值与证据，不必为了凑数把失败路线全量实现。

允许使用共同低模源资产导出多种表示，减少重复制作。源模型须可再编辑、可重导出并登记使用权；Tacview 截图是审美参考，不视为其模型文件的授权。

## 5. 验收标准（Acceptance）

### 5.1 功能与视觉门

- [ ] E0：范围矩阵、全部正式装备到展示模块的映射、现有互斥和待适配项可审计；新文档登记且链接有效。
- [ ] E1：布局确定性；多装备无互斥占位；发射不改变装备槽位归属；主/副库存、齐射、零库存、真实装填、补给、容量变化、无耗弹、暂停/JAM、LOD 重入均走实际权威入口。
- [ ] E1：死亡/部件损毁/切控/换帅/换型/阵营转换/场景退出后，至少跨过一个 SceneTree 帧和下一消费者 tick，断言信号、权威状态、缓存清理以及测试执行到末尾。
- [ ] E2：最窄 focused 通过后运行默认 `all`，包含 `lifecycle_gauntlet`；新增终态分支入门。wrapper 退出 86 即失败。
- [ ] E3：在真实 Survivor 渲染链提供旧/新切换，保留同一批正式实体；正常俯视、近景、远景均可比较，不只展示模型转台或低角度相机。
- [ ] E3：样本覆盖轻装、多装备、不同合法互斥分支；连续展示转向/滚转、离架、空架和实际补满；覆盖地面炮塔、舰船挂点及在飞弹体。
- [ ] E3：机型可辨、体积清晰、挂载不乱串；切档无明显跳变；状态栏、目标线、敌我识别、隐形和危险反馈保持正确。
- [x] E3：2026-09-05 用户亲自试玩后明确满意，批准飞机与地图并行制作；此为初版路线认可，不替代新批次与性能验收。
- [ ] 技术边界：SEAM-019 玩家引用、SEAM-035 视觉 RNG、SEAM-036 屏幕 UI/世界连线均有对应检查；新路径不反向写玩法。
- [ ] 新增玩家可见文字走 i18n；既有世界身份/战术读数的英文规范保持。Debug 不伪造实际触发条件。

### 5.2 性能负载合同

遵守 [性能守则](../../reference/performance-guidelines.md)。本改动可能同时增加 draw 提交、GPU fill、图集/显存、透明合成、按实体更新和创建/释放尖峰，不能用单机面数推算整场 FPS。

| 维度 | 规定 |
|---|---|
| 基线 | 当前正式二维绘制，而不是假定“只有一张 PNG”的旧版本；同机、同 Godot、GL Compatibility、地图、seed、build、相机轨迹、时长、窗口/分辨率、UI 与缓存条件 |
| C1 | `battlefield_atmosphere_stress_36`，30 秒，36 名 / 8 km 混合全可见负载，`Shadow Visual` |
| C2 | `battlefield_atmosphere_stress_48_24km`，30 秒，48 名 / 24 km 多战线及 LOD，`Shadow Visual` |
| S1 | `naval_zone_stress` 或正式武器专项，必须真正激活多挂载、导弹、火箭/炸弹和对应火力热点 |
| S2 | 接入 BOSS / 超级单位候选后跑对应最坏阶段及 Adds 专项；未跑不得宣称覆盖 BOSS |
| S3 | 新渲染目标/图集预热、冷启动、缩放/LOD/隐形切换与 UI 合成的同条件 A/B |
| 可比性 | 记录实际飞机、旋翼、地面、舰船/挂点、在飞弹体、发射/命中及功能激活；同时记录各后端/LOD 的真实覆盖数与回退数 |
| 镜头与采样 | C1/C2 完成标准 `camera_patrol=on segments=8/8`；前三秒预热与稳态分开；旧/新同条件各连续三次 |
| 结果 | 每次 avg/p1/worst/低于 60 帧数、PerfBuckets 热点、CPU/GPU 可用指标、资源/内存峰值及残留；不把不可获得的 GPU 指标伪写成 0 |

性能通过须同时达到 §2.1 底线及相对回退限制；三次中位数只用于比较趋势，不能隐藏某次低于 60 的失败。基线低于 60 是已有性能债，不构成新方案豁免。冷启动/切档低于 60 必须单列，连续三次复现即按守则失败。

混战必须保留真实 AI、伤害、自然交战与反馈；不能用无敌模型展台、静止单位、少刷怪、降低武器频率或提前减员卸掉压力。候选必须实际启用且达到预先登记的覆盖合同，不能绝大多数回退旧模型后宣称新方案过门。自然战斗出现负载分歧时先核实原因和可比性，不直接比较 FPS。

正式推广前另需 E4 / L1 完整局，覆盖高密度战区、装备获得、进化/切控、Tab、任务、BOSS 与结算。短原型通过仅证明该样本，不代替完整局与全品类覆盖。

### 5.3 证据记录

| 等级 | 本版状态 | 结论边界 |
|---|---|---|
| E0 | 2026-09-05：`verify_docs.ps1` 通过；本 spec 无行号锚点，定向锚点检查扫描 0 项；玩家引用持有者审计漏登记 0 | 只证明现有文档/登记健康，不证明候选实现或运行 |
| E1 / E2 | 批次 A focused 55/55（含三机几何与地图 19/19）；最新 all 的 92 项中 1 项失败：地图首次预热 1650 ms 超过 1600 ms 门槛，wrapper 退出 1；另行生命周期原案例 86/86、新增案例 5/5，退出 0 | 不把历史通过覆盖最新失败；几何/投影/铁路与释放回退证据不等于全量装备与终局覆盖 |
| E3 | 用户已认可初版效果；本批三机/地图已有观察图与 GLB；手动 Visual 26/26；C1 三轮与 C2 单轮候选未破 60，但 C1/C2 没有首批专属机型。另做真实 F-47 + Mother Goose 120 秒，候选 3 帧低于 60、最慢 30 FPS，旧原型同档案 0 帧低于 60 | 仅资产/实验接线完成，性能门仍失败；不推广。详见 [批次 A 报告](../../planning/volume-batch-a-results.md)；自然战斗终态不同，不把差异直接归因为模型或地图 |
| E4 | 未开始 | 尚未完成全局迁移或完整局验收 |

候选结果需要并列记录效果、CPU/GPU/资源成本、首次加载/切换、挂载适配难度、资产制作与维护时间。最终决策可为选中一路、继续迭代或保留当前二维方案；不预设一定上线。

## 6. 实现计划（Task Pipeline）

### 本次交付边界与 BOSS 覆盖计划

本次实现隔离显示层和可看的技术资产，不把 §6 后续生产任务自动标记完成。正式入口保持不变。

### 并行制作批次 A（用户已批准开始制作）

- 飞机线由独立 agent 制作 F-14、A-10、F-47 三种专属低模，替换本批机型的通用代理；逐机型登记轮廓、机身/座舱、发动机与尾翼结构，使用项目已有轮廓作为比例依据，不改参数资源、武器或飞机尺度。其它机型保留现有回退，不能把通用代理称作专属模型完成。
- 首批每种单材质、最多 600 三角形、中心与北向保持原规范，共享缓存与按机型批绘，不新增逐实体节点、视口或 tick。该上限是本批制作预算，不是整场性能已通过的承诺。保留 Mother Goose 本体及真实挂点。
- 地图线先完善当前沙漠三维样区：读取现有 MapDocument 的地表范围/配色、道路、铁路、机场与建筑轮廓/高度；明确屋顶、侧墙、跑道和道床，铁路连续连接且沿同一源路线采样。只生产静态显示几何，不新造平行地理坐标或改变碰撞/战区/列车玩法。
- 地图与机体模块分文件拥有，主 agent 统一接线与维护规格/索引。Godot/import/bench 必须在全部 agent 停止写文件后串行运行；不能因并行制作绕过共享锁。
- 验收：focused 检查唯一机型几何、预算、法线、映射、地图来源一致与铁路接缝；在观察场及手动战斗展示本批内容并导出可编辑网格。保持 C1/C2 巡检与地图/近景专项、默认 all 和生命周期回归；新增成本需保留实际样本与已知 120 秒失败，不用短测替代 60 FPS 毕业。
- 本批是生产起点，不把全部机型、地图、航空挂载/CD 或 BOSS 清单标为完成。用户对原型效果的认可已记录，新资产仍须另作效果验收。

| 对象 | 当前状态 | 推广前必须补齐 |
|---|---|---|
| Mother Goose | 首批真实低模本体、8 螺旋桨/2 VLS 真实挂点适配、可编辑 GLB、真实战斗/部件死亡回退 | 多阶段 JAM/弱点/MQ-X 压力、全套三维毁伤/残骸、轮廓与材质艺术验收 |
| Wraith Squadron / The Crucible | 已列入，不以普通固定翼代理视为完成 | 每个成员与命名 Ace 的独立机型、身份、隐形/机动；多阵营多战线最坏负载 |
| Black Star / Hyper-A | 已列入，保留原特殊绘制 | 各代结构和分裂/阶段状态、跨代武器与死亡，不能通用飞机套壳 |
| Carrier Strike Group | 已列入，舰船仍是旧显示 | 航母/护航舰舰体、甲板驻机/弹射、可伤害 CIWS/VLS、沉没与空中后继阶段 |
| Armored Train | 铁路几何已与正式 route 同源；列车模型未替换 | 各车厢、真实连接与转弯、武器分件、尾节断裂、终点失败条件及铁路视觉对齐 |
| Behemoth Land Carrier | 已列入，未替换 | 陆舰本体、移动底盘、炮塔/防空/机库、甲板驻机与起飞、模块死亡与完整 BOSS 流程 |
| Sentinel / 其它超级单位 | 不因不在 BossRegistry 而漏掉；目前回退或技术代理，不算完成 | 独立结构、光环/护卫/数据链、关键单位细节豁免、单位切换后的身份 |

下一轮先拆分地图/阴影/机体与共享战斗的长时尖峰来源，解决 60 FPS 门；在用户认可本次实际画面后，资产优先做 **Mother Goose 美术修订 + 一架玩家机真实多挂载/弹药 CD 恢复**。再接旋翼、地面/舰船/在飞武器，最后按上表逐 BOSS 推广。地图另补分块/裁剪、完整地理、建筑碰撞 SSOT、实际列车与最低目标配置测试。不先宣布全游戏重构完成。

### 阶段 1 — 最小视觉比较

- [ ] 固化同条件基线和样本名单；盘点正式机型/武器来源与现有显示尺寸，不冻结全游戏资产总数。
- [ ] 制作可复用的少量低模源资产；先验证一架固定翼在正常俯视、滚转和近景中的体积收益。
- [ ] 为候选记录具体几何、采样、材质、缓存/渲染目标预算和锚点约定，建立隔离实验开关，不改正式默认。
- [ ] 接入至少两个候选或记录提前淘汰的证据；提供实际旧/新对照。此阶段可淘汰效果不合格路线，不扩成全量制作。

### 阶段 2 — 装备与跨兵种垂直切片

- [ ] 建立只读快照、稳定挂点及真实发射/补弹映射；先通过轻装、多装备和互斥分支测试，再扩展兵种样本。
- [ ] 接入直升机/无人机、地面、舰船和飞行武器样本，验证局部姿态、部件、毁伤、遮挡、标签与 LOD。
- [ ] 补真实终态和 Debug 可达入口；focused → `bench\run.cmd all 1 300 Shadow Headless`。

### 阶段 3 — 密集实战选型

- [ ] 按负载合同跑 C1/C2、S1 与适用 S2/S3；只经 `bench/run.cmd` / `bench/run.sh` 使用隔离副本、锁和有限超时，不绕过错误门。
- [ ] 回填候选参数、Visual 证据、失败与回退；给出用户可看的比较结果和是否值得上线的结论。
- [ ] 以实际样本工时估算剩余资产量、特殊结构适配与回归成本，区分“资产生产”“渲染接线”“全量验收”；不把全部工作称为重构整个游戏。

### 阶段 4 — 另行批准后的推广

- [ ] 用户确认效果和取舍后，明确正式后端、资产标准、覆盖批次和对应 spec 修订；未确认不切换正式默认。
- [ ] 按类别补齐而非一键替换全部 PNG；保留未知/UGC/未适配类型回退与诊断，维护覆盖矩阵。
- [ ] 完成全量数据审计、全部适用性能门及完整局，才声明本批可发布。原型毕业不等于这一步完成。

## 7. 索引锚点（Where）

以下同时登记现有权威接入点与本次已实现的实验入口；是否完成相应范围仍以 §5 证据为准，并非全量覆盖声明。本节不写行号，精确位置由 reference 索引维护。

| 关注点 | 文件与符号 |
|---|---|
| 正式飞机绘制与姿态 | `scripts/aircraft_renderer.gd`：`draw_aircraft_icon`、`bank_volume_projection_for` |
| 本次三维候选 | `scripts/experiments/volume_world_probe.gd`：`setup`、`_sync_camera`、`_sync_goose_mounts`、`summary`、`_exit_tree`；`volume_mesh_factory.gd`：`aircraft`、`map_mesh` |
| 首批资产制作 | `scripts/experiments/volume_aircraft_catalog.gd`：`supported_keys`、`build`、`metadata_for`；`volume_map_factory.gd`：`build`、`stroke_edges`、`sleeper_poses`、`airport_polygon` |
| 观察 / 导出 / 验证 | `scenes/tests/volume_3d_lab.tscn`；`scripts/bench/bench_runner.gd`：`VOLUME_SCENARIOS`、`_swap_to_volume_experiment`；`test_volume_world.gd`、`volume_world_lifecycle_case.gd` |
| 手动实验战斗 | `scenes/tests/volume_3d_combat.tscn` → `scripts/experiments/volume_3d_combat.gd`：`_ready`；`scripts/tests/volume_combat_smoke.gd`：`run` |
| 机型轮廓与别名 | `scripts/aircraft_silhouette_catalog.gd`：`key_for`、`draw_icon` |
| 主/副弹发射及装填 | `scripts/aircraft/aircraft_weapons.gd`：`update_missile`、`update_secondary_missile`、`_emit_missile`、`_finish_main_missile_cycle`；还须审计调用之外的特殊发射/补给路径 |
| 飞机状态与其它入口 | `scripts/aircraft.gd`：主/副库存与装填状态 |
| 武器库与继承 | `scripts/survivor/survivor_player.gd`：`record_special_weapons`、`remount_weapons` |
| 玩家引用转移 | `scripts/survivor/survivor_mode.gd`：`_set_player_aircraft` |
| 在飞武器 | `scripts/missile_manager.gd`：`spawn_missile`；`scripts/missile.gd`；`scripts/rendering/bullet_presenter.gd`：`draw` |
| 舰船与真实挂点 | `scripts/naval/naval_unit.gd`：`_draw_alive_mount` |
| 独立世界状态栏 | `scripts/ui/world_unit_status_overlay.gd` |
| 运行与静态验证 | `bench/run.cmd`、`tools/verify_player_ref_holders.py`、`tools/verify_doc_anchors.py`、`tools/verify_docs.ps1` |
| 进一步定位 | [Script Index](../../reference/script-index.md)、[Code Index](../../reference/code-index.md)、[Known Seams](../../architecture/known-seams.md) |

## 8. 变更记录

| 日期 | spec_version | 改动 |
|---|---|---|
| 2026-09-05 | 4 | 用户满意实战效果并授权并行制作专属飞机模型与地图；首批 F-14/A-10/F-47 和沙漠静态样区，严格保持玩法权威、原型隔离和性能门。 |
| 2026-09-05 | 3 | 用户要求在实验场景亲自战斗：新增 F6 手动 Mother Goose 实验战入口和观察场 B 键；复用非 bench 的正式操控、Boss Debug 构筑/终态/重开，不强制相机或自动退出；不改变长时性能未通过的裁决。 |
| 2026-09-05 | 2 | 用户批准实施可行性测试，并追加地图三维候选与 Mother Goose 首批模型。保留二维模拟；明确单共享视图、同源投影与铁路、真实挂点及成对性能边界；未批准全面上线。 |
| 2026-09-05 | 1 | 将已确认的 2D 世界内战斗实体立体表现、稳定多武器挂载、真实补弹、普通单位视觉 LOD 与 60 FPS 混战底线写为原型契约；具体渲染路线和全量推广保留为效果/性能验证后的决定。 |

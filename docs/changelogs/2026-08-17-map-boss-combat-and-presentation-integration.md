# 2026-08-17 地图、BOSS、战斗与演出整合

## 范围

- 汇总并发布当前 `main` 上尚未推送的本地集成历史，以及本轮工作树中的地图、BOSS、技能、武器、飞行表现、UI 演出和文档更新。
- 保留 `logs/`、`bench/results/` 与 `tmp/` 中的本机运行记录、截图和中间产物；这些目录继续按仓库约定忽略，不进入发布提交。
- 本批设计与实现仍以 `docs/specs/` 为权威；`docs/reference/` 只维护代码入口、资源和工作流指针。

## 玩家可见变化

### 地图与缩放

- 东京湾、沙漠铁路、海岛三张底图清理了行政边界，并保留原始生产 PNG 作为可回滚来源。
- 新增三层无损 WebP 分块金字塔、清单、共享蓝噪声和流式底图渲染器；主地图、战术地图、缩放与 LOD 共用同一套地图数据。
- 三图共 442 张 WebP，分块 QA 的接缝、父层均值与 68 MiB 磁盘预算门全部通过；当前总量为 67.872 MiB。

### BOSS 与演出

- Black Star / Hyper-A 完成 `1 → 2 → 4 → 8` 分裂代际、再入/冲刺、威胁覆盖层、正式轮换、Debug 选择器和生命周期/压力回归。
- Mother Goose 接入 GOOSE-VLS 远距空爆与近距安全区，MQ-111 使用累计导弹拦截生命值，并补齐 BOSS 进度与战斗流程回归。
- 新增终端系统警告风格的 BOSS 到达横幅，保留名称、类型、呼号、英文箴言和可配置配色；五份 i18n 表与多 BOSS Visual 截图同步更新。

### 战斗、技能与飞行表现

- 新增并完成“高度能量循环”“狂化病毒”“火控饱和”及高度动作状态，运行时 buff 通过统一 effective accessor 透传到 AI 战术层。
- 无制导火箭改为左右挂点逐发、先平行直飞再平滑展开散布，敌我共用同一轨迹语义。
- 飞机俯视轮廓补齐侧倾体积、近镜头数据标签半径锚定和对应 Visual/性能探针，改善大图标下的重叠、抖动和扁平感。
- 同步机炮、导弹规避、编队、天气、战区支援、功勋/生涯档案、胜利推进与调试可达性相关修正。

### 本地集成历史

- 纳入终端 HUD 首次显现、模块化飞行仪表、单位标签配色、屏幕边缘状态反馈和无线电/i18n 分表更新。
- 纳入导弹发射纪律和雷达/子弹性能改进；对已被当前实现取代的 UI / Sprite 分支只闭合 Git ancestry，不回退现行内容。

## 验证与证据

- `map_raster_stress`（Godot 4.7.1 Shadow，10 秒）：52 架飞机，平均/1% 低帧 144.73 FPS，最差帧 111.33 FPS，低于 60 FPS 帧数为 0。
- `boss_hyper_a_lifecycle`：分裂生命周期结束后 BOSS 清理完成；`boss_hyper_a_stress` 与对应 Visual 截图已生成。
- `berserk_virus_baseline` / `berserk_virus_stress`：31 架规模的基线与技能压力样本均完成；压力样本 15 秒内产生实际击杀。
- `aircraft_bank_volume_visual`：Visual 截图已生成；3150 次绘制样本中体积侧倾相对平面绘制增加约 1.335 微秒/次。
- `aircraft_label_visual`、`rocket_trajectory_visual`、`boss_arrival_banner_visual`、`boss_debug_select_visual` 已生成可检查截图。
- 发布前补跑玩家引用持有者审计、文档锚点、文档一致性与 `git diff --check`；最终结果以提交/推送回执为准。

## 已知边界

- 流式地图已完成无损金字塔 QA、Visual 与暖缓存 52 架压力测试；首次安装/清空缓存后的冷缓存加载风险仍需在导出包上单独验收。
- `logs/`、`bench/results/` 和 `tmp/` 是本机证据与中间产物，不上传；可重建的正式测试场景、脚本、地图 QA 摘要和生产资源随代码提交。
- 现有保护 stash 保留，不把历史混合 stash 自动并入当前工作树。

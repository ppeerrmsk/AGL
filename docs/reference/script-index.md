# Script Index — 关键文件职责

> 本节内容原在 CLAUDE.md，2026-05-05 移出。

**模式归属图例**：`[共享]` = 共享层；`[沙盒]` = 只在沙盒模式（**沙盒已废弃**，仅调试留存，验证不必再跑它）；`[生存]` = 只在生存模式（主玩法）。

**禁止**在共享层代码里写 `if in_survivor_mode` / `if in_sandbox`——模式隔离必须走参数资源 `duplicate(true)` 或 PlayableAircraft 档案注入。这条铁律在沙盒废弃后依然有效（它保护的是共享层干净，不是两模式对等）。

| 文件 | 类/类型 | 职责 | 关键入口 |
|------|---------|------|----------|
| `aircraft.gd` | `Aircraft extends CombatUnit` | [共享] 飞机主控（~2660 行，LOD 路由 + 损伤生命期 + 状态所有者；物理/武器/战斗/编队/热诱弹全部委托 aircraft/ 子模块） | `_physics_process` `take_damage` `_apply_damage` `_check_ground_crash` `_start_destroy` `_update_cobra_skill` `_update_evasion_herbst_skill` `_update_manual_dodge_skill` `try_manual_maneuver` `do_manual_dodge` `is_manual_maneuver_controlled` `set_evasion_mode` `_update_evasion` `get_maneuver` `is_lock_immune`（含 `sensor_hidden`）`get_flare_cooldown_ratio` `_draw()`（传感器隐藏时抑制本体绘制）+ 敌机一梭安全门状态/纯查询 `_ai_gun_pause_timer` / `_ai_gun_burst_allowed` + 5 个委托壳（`_missile_cannot_hit_but_gun_can` `_should_commit_gun_pass` `_is_gun_pass_finished` `_is_in_missile_envelope` `_choose_dogfight_pursuit_pos`）+ 2 个物理委托壳（`_effective_max_g` `_max_bank_angle_at_speed`） + 编队接口 `set_formation_target(leader, slot, keep_arrival)` / `clear_formation()`（2026-05-04 重构）+ `_ai_ref`（AIController 反向引用，由 AI._ready 设；`manual_control` 是 R 手动/AI 威胁自动机动分流真源）+ `_publish_equipment_to_legacy()`（commit 2/13 起）+ 意图仲裁 `submit_intent(source, ci)` / `withdraw_intent(source)` / `_resolve_intents(delta)` + 敌方护卫反应 `escort_guards` / `_alert_escort_guards()` + 机炮威胁锥淡出 `_gun_threat_fade` / `_update_gun_threat_indicator()` / `_reset_gun_threat()` |
| `aircraft.gd`（主动特殊机动） | `ActiveSpecialManeuver` | [共享] 五向 R 入口的共享协调器；位移滚转/垂直越过动作曲线、10Hz AI 威胁门、命令排队、命中资格与共享冷却接线 | `aircraft.gd:2142 is_active_special_maneuver` `aircraft.gd:2146 can_accept_new_hit` `aircraft.gd:2207 _try_start_displacement_roll` `aircraft.gd:2232 _try_start_vertical_break` `aircraft.gd:2337 _update_active_special_maneuver` `aircraft.gd:2357 try_manual_maneuver` |
| `aircraft/control_intent.gd` | `ControlIntent extends RefCounted` | [共享] 控制意图值类型（Phase 1 重构，docs/planning/physics-ai-control-refactor.md §5）：SOURCE_TACTIC/EVADE/BRAKE + PRIORITY 表；哨兵 pursuit=INF / speed<0 / ab<0 = 不主张 | 字段 `pursuit_pos` `clear_pursuit` `pursuit_pri`（BRAKE=25 特例） `target_speed_kmh` `afterburner` |
| `aircraft/aircraft_flares.gd` | `AircraftFlares extends RefCounted` | [共享] 热诱弹子系统（静态方法首参 `ac: Aircraft`）；含僚机护卫 flare；AI 僚机机动就绪时让位、当前操控机未按 R 时不压制自动 flare | `update(ac, delta)` `release(ac, target_missile)` `try_cover_flare(ac, leader)` `release_cover(ac, leader, missile, d_leader_m)` `player_flare_should_trigger(ac, m)` `flare_ready(ac)`（就绪门，护卫/自卫/B1 规避入口三方共用） `calc_jam_chance(ac, m)` `cooldown_ratio(ac)` + `FLARE_*` / `ESCORT_*` / `MISSILE_PHASE_DURATION` 常量 |
| `aircraft/aircraft_weapons.gd` | `AircraftWeapons extends RefCounted` | [共享] 武器子系统（~1247 行；2026-05-10 新增副导弹槽独立子系统） | `update_weapon_mode(ac)` `auto_gun_scan(ac)`（普通机炮仅 Aircraft 且守交战纪律；炮艇为玩家全队各机独立 3Hz 最近 Aircraft/GroundUnit 扫描，不锁 planner 目标池）`update_passive_gunship(ac, delta)`（编队/LOD 提前返回路径仍 tick 独立炮塔）`update_gun(ac, delta)`（梭射状态机 + 目标实例锁存/逐 tick 提前点刷新 + 全敌方 Aircraft 一次机会一梭/末发后停火 3s，spec: weapons/gun-burst-fire）`_gun_target_from_id` `_refresh_committed_gun_aim` `_fire_gun_round(ac, gun)`（炮艇炮口随射向旋转）`update_ciws(ac, delta)`（独立正面 5° 锥 + `CIWS_FIRE` 诊断）`update_rocket(ac, delta)` `update_formation_passive_missile(ac, delta)`（玩家方编队 20Hz 机会火控，spec: waypoint-fire-control）`update_missile(ac, delta)`（2026-07-12 火力纪律：超杀记账含自身在飞弹 + AI 机炮让路门，spec: weapon-employment-doctrine v7，bench `fire_discipline`）`update_secondary_radar(ac, delta)` `update_secondary_missile(ac, delta)` + 内部 `_launch_rocket` `_update_weapon_mode_tactical` `_fire_missile_at(..., is_secondary)` `_fire_multi_lock_salvo` `_pick_secondary_target` `_pick_dogfight_side` `_pick_closest_locked` `_is_valid_secondary_candidate` `_secondary_target_class_bit` |
| `aircraft/aircraft_weapons.gd`（锁数齐射） | `_fire_multi_lock_salvo` / `_salvo_fire_count` | [共享] 一轮覆盖数取有效锁数、合法目标与弹量最小值；每轮统一进入正常导弹冷却 | `aircraft/aircraft_weapons.gd:1108 _fire_multi_lock_salvo` `aircraft/aircraft_weapons.gd:1290 _salvo_fire_count` |
| `aircraft/aircraft_physics.gd` | `AircraftPhysics extends RefCounted` | [共享] 物理子系统（~1680 行，每帧演算 + 查询函数 + `effective_*()` buff 注入层） | `update_target_heading(ac)` `update_bank(ac, delta)` `update_heading(ac, delta)` `update_speed(ac, delta)` `update_altitude(ac, delta)` `update_stall(ac)` `update_g_load(ac)` `apply_movement(ac, delta)` `update_rotorcraft(ac, delta):1625` `update_shock_absorb(ac, delta)` `update_fuel(ac, delta)` `update_energy_management(ac)` `set_afterburner(ac, on)` + 机动守卫 `maneuver_overrides_speed(ac)` / `maneuver_overrides_g(ac)`（B2 统一 Cobra/Herbst 让位判定，新机动模块只改这两处，2026-07-03） + 查询 `max_bank_angle` `effective_max_g` `corner_speed_kmh` `stall_speed` `max_speed_at_altitude` `air_density_ratio` `max_bank_angle_at_speed` |
| `aircraft/aircraft_combat_tracking.gd` | `AircraftCombatTracking extends RefCounted` | [共享] 战斗追踪子系统（~654 行，BFM 级目标追踪 + 武器模式判定） | `update_combat(ac, delta)` `update_combat_ground_attack(ac)` `choose_dogfight_pursuit_pos(ac, ...)` `missile_cannot_hit_but_gun_can(ac)` `should_commit_gun_pass(ac)` `is_gun_pass_finished(ac)` `is_in_missile_envelope(ac, tgt, msl)` + 内部日志 `_log_pursuit_snapshot` `_log_enemy_squads_engaging_player` |
| `aircraft/aircraft_formation.gd` | `AircraftFormation extends RefCounted` | [共享] LOD 1 编队托管子系统（阶段流水线；顶部有架构图 + bug 回溯地图 10 条）| `update_follow(ac, delta)` 阶段流水线：`_build_context` → `_resolve_target_heading` → `_update_bank_via_pd`（PD 航向+bank 一体，与战斗/巡航同源） → `_update_speed`(20Hz) → `_log_formation_debug` → `_update_altitude`(20Hz) → `_update_position` → `_periodic_and_visuals`；`Branch` 枚举（旧编队私有 `_update_heading`/`_update_bank`/leader-bank 混合/翻转守卫已随 PD 重写退役，死代码 2026-07-03 删除） |
| `aircraft_renderer.gd` | `AircraftRenderer extends RefCounted` | [共享] 飞机绘制系统；含炮艇 360°机炮射程圈、ESM 3000m 光环与常规轮廓/锁定/尾迹等批绘 | `draw_gun_cone` `draw_esm_aura` `draw_radar_cone` `draw_lock_indicator` `draw_aircraft_icon` `draw_data_label` `draw_predicted_path` `draw_formation_debug` |
| `aircraft_params.gd` | `AircraftParams extends Resource` | [共享] 飞机数据；含固定翼/旋翼机 flight model、旋翼机角色及真实视觉尺寸 | `FlightModel` `RotorcraftRole` `visual_length_m` `visual_span_m` |
| `aircraft_destruction.gd` | `AircraftDestruction extends RefCounted` | [共享] 坠毁动画系统（fighter/bomber/heli 三种风格） | `start(ac)` `update(ac, delta)` |
| `cobra_maneuver.gd` | `CobraManeuver extends Node` | [共享] 眼镜蛇机动模块（挂载到 Aircraft 子节点） | `activate` `_physics_process`（三阶段状态机） |
| `herbst_maneuver.gd` | `HerbstManeuver extends Node` | [共享] 赫尔贝特轮 J-Turn 模块；默认可重复，profile 可用 `max_uses` / `requires_flares_empty` 限制次数与 flare 分层（WhiteTea 每机一次） | `activate` `_physics_process`（DECEL→TURN→ACCEL） |
| `ai/tactical/engagement_speed_governor.gd` | `EngagementSpeedGovernor extends RefCounted`（全静态） | [共享] 交战速度治理：反解"要咬住 dist 外目标速度最多多少"，压掉"盘旋半径 > 交战距离 → 机头几何上指不到 → 满 G 绕圈"死结（playtest log 20260720_172222）。地板=角点速度，绝不压到失速螺旋；只改 plan 速度主张，不碰 G/bank/几何。目前只对王牌中队生效 | 常量 `RADIUS_RATIO=0.5` `MAX_GOVERN_DIST_M=6000`；`speed_cap_kmh(dist,max_g,corner)` `apply(s,p)` `is_governed(ac)`；接线在 `aircraft.gd` planner 之后一行 |
| `survivor/ace_tier.gd` | `AceTier extends RefCounted`（全静态） | [生存] **王牌中队 tier 语义的单一归属地**（spec systems/ace-squadron-tier）；收敛原本散在 LOD/缩放/HP cap 三处的判定，调用方只问不判；BOSS ⊂ 王牌中队，tier 待遇一律走本模块不看 `category=="boss"` | 常量 `TIER_META` `TIER_ACE` `MAX_HP=100`；`is_ace_type`（成员名单，加新王牌中队只改这里）`mark` `is_ace` `no_scale` `exempt_from_hp_cap` `apply_hp` |
| `survivor/ace_squad.gd` | `AceSquad extends BossEncounter` | [生存] 王牌中队 BOSS 基类（飞机 BOSS 框架）；纯**小队级状态机**，状态切换才改 AI 字段，不每帧覆盖 → 杜绝 BFM 战术 timer 抖动；自管世界边缘软收容（2000px 触发→3000px 安全带），并在 40px 触线前硬钳；只守世界外框、**不是锚点 leash** | 状态：`INTRO/PURSUIT/CLOAK`（**无归巢态**，ANCHOR_HOLD 已删）；角色 `AceRole{NONE,KNIGHT,SNIPER}` + `ROLE_META` + `role_of()`；边缘入口 `_update_boundary_recovery` / 纯几何 `boundary_recovery_point`；钩子 `_pursuit_enter` `_pursuit_update`（每 0.5s 软重连）`_cloak_enter/exit/update`；`spawn` `update` `engage` `set_player_ref` `_apply_role` |
| `survivor/boss_encounter.gd` | `BossEncounter extends RefCounted` | [生存] BOSS encounter 统一接口 + 通关强化次数载体；`prior_defeats` 只在 spawn 前注入，保留完整历史次数供未来分层 | `configure_progression(defeat_count)` `spawn` `update` `engage` `is_defeated` `set_player_ref` |
| `survivor/wraith_tactics.gd` | `WraithTactics extends RefCounted` | [生存] WRAITH 队级战术状态机（spec wraith-squadron §2.3）：PERCH→BRACKET→PRESS→RESET 四相闭环 + 退化检测。**不每帧覆盖 AI 字段**——走位下 AIDirective、包夹写 surround_bearing、PRESS 相完全放手 | `setup` `start` `stop` `update` `_decide_next` `_enter_phase/_exit_phase/_update_phase` `_perch_enter` `_bracket_enter/_bracket_update` `_reset_enter` `_tick_degradation` **纯函数** `pick_bait` `wing_bearings` `axis_heading` `is_biting` `average_nose_off_deg` `perch_tier_for` |
| `survivor/f47_ace_squad.gd` | `F47AceSquad extends AceSquad` | [生存] Wraith 四机战术实现；首败后在 `engage` 追加 2 架雷达静默 YF-23 可选支援，不进演出/血条/胜利判定，切控时随 encounter 重定向 | `spawn` `engage` `set_player_ref` `support_count_for_progression` `_spawn_progression_support` `_configure_spawn` `_configure_close_fighter_combat` `_configure_ranged_striker_combat` |
| `survivor/carrier_strike_group.gd` | `CarrierStrikeGroup extends BossEncounter` | [生存] LADON 航母战斗群两阶段 BOSS；通关分层只改 Phase 1 护航编成（初见 0CG+2DDG+6FFG，首败后 2CG+2DDG+8FFG），摆位按实际编成校验水域 | `spawn` `update` `engage` `set_player_ref` `escort_counts_for_progression` `_build_escort_plan` `_pick_water_placement` |
| `survivor/poltergeist_squad.gd` | `PoltergeistSquad extends AceSquad` | [生存] CSG 二阶段 F-14 母舰死守小队；弹射四阶段状态机（FROZEN→FADING→CATAPULT→FLYING）+ 起飞保护期 + 覆写 `_tactics_*` 钩子接 PoltergeistTactics | `spawn` `update` `_tick_phase` `_tactics_enter/_update/_exit` `_configure_spawn` |
| `survivor/poltergeist_tactics.gd` | `PoltergeistTactics extends RefCounted` | [生存] POLTERGEIST 队级战术（spec bosses/poltergeist-squadron）：**死锁单机换手**——共速绕圈达阈值 → 只把最咬不住的**1 架**拽去爬升拉开重整、其余继续压（同时换手上限=1）。区别于 Wraith 全队 RESET；不每帧覆盖 AI 字段，走位下 AIDirective | `setup` `start` `stop` `update` `_sample_and_dispatch` `_begin_reset` `_tick_active_resets` **纯函数** `nose_off_deg` |
| `survivor/ace_squad_profiles.gd` | `AceSquadProfiles`（全静态注册表） | [生存] 六支非宿敌王牌（七队含 ORION）的编成/包装/flare/机动/TTK 预算 SSOT；240s 统一池、新局无放回洗牌、DU 与预计击破时间公式 | `pool_at` `build_run_order` `defeat_units` `estimated_ttk_s` `reserve_callsigns` |
| `survivor/ace_support_squad.gd` | `AceSupportSquad extends AceSquad` | [生存] **非 BOSS 王牌支援中队**：profile 驱动混编/装备/flare/专属机炮资源；含 WhiteTea 逐机 `gun_lancer` joust + 单次 J-turn 注入 | `_init` `_configure_spawn` `_element_of` `_member_type` `_member_role` `is_ammo_dry` |
| `survivor/mother_goose_boss.gd` | `MotherGooseBoss extends BossEncounter` | [生存] Mother Goose 空中航母 BOSS（10 挂点+弱点、UAV 蜂群、JAM、VLS）；初见向蜂群关闭 MQ-111/112，首败后恢复完整型号池 | `spawn` `update` `engage` `get_display_members` `_assign_patrol_ring` |
| `survivor/mother_goose_controller.gd` | `MotherGooseController extends Node2D` | [生存] Mother Goose 战斗逻辑控制器（boss 子节点）：每帧 tick jam_shield + uav_swarm + 给场内玩家施加 jam 效果 + 绘制 shield 圆环；2026-05-12 加 SwarmDirector lazy init + 1Hz tick + ATTACKER 角色跳 stray recall | `_process` `_draw` `_apply_jam_field` `_clear_jam_field` `_init_swarm_director` `_update_stray_recall` `_update_designation` |
| `survivor/mother_goose_jam_shield.gd` | `MotherGooseJamShield extends RefCounted` | [生存] 周期性干扰场状态机：COOLDOWN 60s → WARNING 4s → EXPANDING 8s（R=300→1500） → SUSTAIN 8s；完整循环 80s | `start` `update` `is_effect_active` `is_warning_active` `contains_world_point` `draw_into(ci, shield)` |
| `survivor/mother_goose_uav_swarm.gd` | `MotherGooseUAVSwarm extends RefCounted` | [生存] Mother Goose UAV 蜂群管理：起始 12 / 上限 30；常态固定每 20s 补 2，指定猎杀 ACTIVE 内 0.0s/7.5s 两波各 6（固定 2 架/2s drain）；通关门控 MQ-111/112，并仅在开放时执行 MQ-112 最低配额 | `setup` `spawn_initial` `update` `set_replenishing` `request_reinforcement_wave` `stop_replenishing` `alive_count` `variant_weights_for_progression` `_roll_variant` `_spawn_one` |
| `ai/swarm/swarm_director.gd` | `SwarmDirector extends RefCounted` | [生存] Mother Goose UAV 战略层（1Hz）：把 30 架 UAV 分配到 SHOOTER / ATTACKER_N/E/S/W / DECOY / GUARD / RESERVE 角色 + lane 世界角度。AIController.simple_combat 读 swarm_role_override 切楔形攻击点 → 实现 4 轴同步抵达突击。是 Shaw 2v1 Bracket+Sandwich 的 swarm 化身 | `setup` `update(delta)` `_tick` `_pick_shooters` `_pick_attackers` `_pick_decoys` `_pick_guards`；静态 `role_of(uav)` `is_active_attacker(role)` |
| `ai/swarm/swarm_director_params.gd` | `SwarmDirectorParams extends Resource` | [生存] SwarmDirector 可调参数 Resource：策划在 Inspector 里调角色比例、楔形宽、滞回阈值；默认资源 `resources/ace_swarm_director.tres` | `@export var attacker_pct / shooter_count / decoy_pct / guard_pct / wedge_half_deg / converge_distance_* / hysteresis_score_pct ...` |
| `ai/joust_controller.gd` | `JoustController extends RefCounted` | [共享] 攻击跑行为原语（spec joust-attack-run）：RUN_IN 对准进入火力窗（两段速：带外全速/入带稳巡航）→ BREAK 脱离拉开（近图边朝中心 lerp）→ 折返循环；包络动态读装备（railgun>missile>gun）；电磁炮充能保持窗禁切 BREAK；消费方=AIController 双钩子（simple hunter 块 + _process_engage）；MQ-110/112 + Lancer 四型接线 | `update(ai, delta)→bool`（true=movement 已接管）`_resolve_outer_px` `_resolve_inner_px` `_closing_mps` `Phase{RUN_IN,BREAK}`；AIController `joust_*` @export ×7 |
| `rocket_params.gd` | `RocketParams extends Resource` | [共享] 无制导火箭弹参数（齐射数/散布/冷却） | — |
| `ai_controller.gd` | `AIController extends Node` | [共享] AI 主控（状态机路由 + 状态所有者 + BVR/BOSS 覆盖判定 + 简单 AI 路径 `_process_simple` / `_try_engage_simple` / `_try_engage_in_tether_range`；战术/规避/目标/编队全部委托 ai/ 子模块）。**Phase 2 状态正交化（2026-07-05）**：`AIState` 三值 PATROL/ENGAGE/SQUAD_FOLLOW，EVADE = `_evading` modifier（MissileEvasion 独占进出，背景 _state 保持）；全部 _state 写入走过渡函数 | `_physics_process` `_process_patrol` `_process_engage` `_process_simple` **过渡函数** `enter_engage_state(reset_tactic)` / `enter_squad_follow_state(snap)` / `enter_patrol_state(pick_waypoint)` / `is_evading()` **目标仲裁** `get_target_source()` / `acquire_target()` / `release_target()` **约束层** `_apply_constraints(delta)`（分发前统一：combat_zone + 小队 leash 全模态，SEAM-010 根治）`_cmd_engage_active()`（本机点名 + 跟打长机点名目标，豁免普通归队/超距/超时/重评估）`_set_next_waypoint` `_generate_default_waypoints` `_get_missile_manager` `_find_member_ai` `is_boss_attacker` `_enforce_commanded_target`（玩家命令铁律，见 rts-command spec）`request_overkill_retarget`（仅非机炮优先 TS_SCORED 的超杀即时重评，1s 节流）+ `SituationData` 类 + `EngageTactic` 枚举 + joust_* @export / `_slow_air_joust`（≤100m/s 慢速空目标路由 joust，v4；测试 `scripts/tests/test_fire_discipline.gd` bench `fire_discipline`）（spec joust-attack-run）+ 核心状态字段 `_state / _evading / _current_target / _tactic` |
| `ai/bfm_tactics.gd` | `BFMTactics extends RefCounted` | [共享] BFM 战术子系统（~574 行，8 个战术执行器 + 决策树） | `assess_situation(ai)` `choose_tactic(ai, s)` `apply_new_tactic(ai, t)` `make_mistake(ai, correct)` `update_lufberry_detection(ai, s, delta)` + 8 个 `execute_*`（`lead_pursuit` `lag_pursuit` `lead_turn` `high_yoyo` `low_yoyo` `break_turn` `extension` `scissors`）+ 辅助 `match_target_altitude` `set_patrol_altitude` `update_gun_jink` `set_engage_speed` `_apply_fear_daze`（zone_air FEAR 平飞 helper） |
| `ai/target_selection.gd` | `TargetSelection extends RefCounted` | [共享] 目标选择子系统（可命中性评分：对正/包络/锁定封顶/邻近四因子 + 承诺火力超杀让路〔机炮优先豁免〕+ 守后优先；spec target-engageability-selection）+ 战场引力三带/地板/可行性门/带感知粘性接线（spec battlefield-gravity，仅玩家小队 TS_SCORED 生效） | `try_engage(ai)`（交战地板 ENGAGE_MIN_SCORE）`reevaluate_target(ai)`（地板脱离 ×2 评估；机炮优先不因在途弹失粘性）`disengage(ai)` + `_score_candidate(ai, tgt, escort_leader, guard_ctx)`（引力乘子+三带加性+可行性门；机炮优先不吃 OVERKILL_MULT）`_sticky_for(ai)`（生存带 8.0 / 顺手 0.10）`_envelope_factor(ac, tgt, dist_m)` `_is_guard_rear_context(ai)` `_visibility_score_mult(tgt)` + 常量 `W_ALIGN/W_ENV/W_LOCK/W_PROX/OVERKILL_MULT/STICKY_BONUS/REAR_GUARD_PRIORITY/SWITCH_MARGIN_*` |
| `ai/objective_context.gd` | `ObjectiveContext extends RefCounted`（全静态） | [共享] **战场引力共享上下文**（spec battlefield-gravity）：队级"当前主战场"空间锚 + 三带判据。生存模式填充（BOSS 存活质心 / 最近 triggered 战区 / 兜底操控机实时位），沙盒恒 disabled → 全判据无操作。成员集只认实例表（禁 category 字符串匹配，防误伤 ace_support）；生存层只对空（engaging_me 仅飞机写）。`is_objective` / `is_survival_threat` 是生命周期边界，收 `Variant` 后先判实例有效，允许长期缓存的目标引用已经释放；static 跨场景 → 新局 reset+enable、退局 reset（同 UgcLoader 惯例） | 常量 `SURVIVAL_BONUS(100)/OBJECTIVE_BONUS(40)/R_CORE_PX(2000)/R_FAR_PX(6000)/GRAVITY_FLOOR(0.1)/SURVIVAL_RANGE_PX(3000)/ENGAGE_MIN_SCORE(0.15)/LEASH_FEAS_MARGIN(0.9)/SURVIVAL_STICKY(8)/BRACKET_SLACK_PX(1200)`；`reset` `set_boss_objective` `set_zone_objective` `set_no_objective` `effective_anchor` `gravity_mult(pos)` `is_objective(unit: Variant)` `is_survival_threat(cand: Variant)` `threat01` `band_bonus`；状态 `enabled/has_objective/member_ids/anchor/protectee` |
| `ai/missile_evasion.gd` | `MissileEvasion extends RefCounted` | [共享] 导弹规避子系统；scatter-on-broadcast 已废除（spec wingman-escort-evasion）。Phase 2：`_evading` modifier 唯一写入者（enter 幂等 / exit 三路重定走 AIController 过渡函数）；leash 拷贝退役（上收 _apply_constraints） | `process_evade(ai, delta)` `enter_evade(ai)` `exit_evade(ai)` `should_enter_evade(ai)`（B1 分层门）`check_incoming_missile(ai)` `find_nearest_incoming_missile(ai)` `_is_evasion_threat(ac, m, already_evading)` `is_missile_from_rear(ai, missile)` |
| `ai/squad_coordination.gd` | `SquadCoordination extends RefCounted` | [共享] 编队协同子系统 + 护卫学说评分（squad-ai-escort）+ 守后威胁评分（target-engageability-selection）+ 跟打长机目标（玩家点名时标记命令继承语义）+ **生存层主动回防**（spec battlefield-gravity §3.3：SQUAD_FOLLOW 常开 1Hz、锚操控机、绕雷达锥、≤MAX_DEFENDERS(2)） | `process_squad_follow(ai, delta)` `try_defend_protectee(ai)` `_count_defenders(ai, protectee)` `is_escort_wingman(ai)` `escort_target_bonus(leader, candidate)` `scan_leader_rear(ai)`（按 `rear_threat_score` 选最高威胁）`rear_threat_score(leader, candidate)` `scan_squad_nearby_enemy(ai)`（含引力 band_bonus）`end_cover_engagement(ai)` `broadcast_salvo(ai)` `process_salvo(ai, delta)` |
| `rts/squad_command_controller.gd` | `SquadCommandController extends Node` | [生存] RTS 指挥单一所有者（命令 + 自动交战逻辑，从 survivor_mode 抽出）；玩家命令(`commanded_target`)走铁律，自动交战(`_auto_engage_target`)可拴绳；轮盘广播命令 + 队级战术状态也在此（spec rts-command / command-wheel） | `setup(mode, params)` `command_attack(enemy)` `command_move(world_pos)` `cancel()` `tick(delta)` `_find_target(center, radius)`（用 `CombatUnit.all_units`）+ 轮盘广播 `command_regroup` `command_evacuate`（3km 圈外不生效）`command_guard` + `_tick_guard`（区域清剿 standing order：全队逐机自主领圈内目标/认领数让路/击杀后接续/出圈×1.15 放弃/清空回防）+ `_end_guard`（只回收任务借用的铁律目标）`command_attack_all(target, posture)`（FOCUS=广播集火+包围轴 `_assign_surround_axes` / SPREAD=`_begin_spread`+`_tick_spread` 池内各自接敌）`_squad_members` + 状态 `fire_allocation` `formation_tight` `_guard_point` `_guard_owned` `_spread_*` `wheel_params`；姿态/包围字段 `Aircraft.attack_posture` `surround_bearing_rad`（随 commanded_target 走）→ Situation 门控透传 → 空中 STANDOFF 走 `AIController._posture_standoff_air` 路由 joust / 面目标走 ground_strafe 分流 / 包围门点 `TacticalPlanner._apply_surround_axis`；集合/撤离全力加速 `Aircraft.command_sprint`（`_tick_sprint` 到达/出圈解除，注入见 aircraft_physics `COMMAND_SPRINT_MULT`）+ 撤离 20s 禁入区（`_in_evac_zone` 过滤 `_find_target`/拴绳 + 内部类 `EvacZoneMarker` 圈框倒计时）+ TIGHT 整队齐射 `_begin_tight_engage`/`_tick_tight_volley`（触发器=长机开火，开窗 1.5s 授予僚机 `Aircraft.volley_fire_active`+临时 combat_target 在编队槽位开火，SquadCoordination 防御清除豁免该标志；安静期 2s 再武装；ASSAULT 豁免）；验收 `--bench=fire_alloc` / `--bench=wheel_orders` / `--bench=tight_volley`；720 批：`_update_guard_zone_buff`（保卫阵地——防守命令激活时按账本给圈内成员打 `guard_zone_buff_active`，减伤/回转注入见 aircraft `_apply_damage` + aircraft_physics `_g_buff_mult`） |
| `rts/rts_command_params.gd` | `RtsCommandParams extends Resource` | [生存] RTS 自动交战可调参数（去硬编码）；默认资源 `resources/rts_command.tres` | `@export auto_engage_radius_px / auto_engage_leash_mult / auto_engage_interval_s` |
| `rts/command_wheel.gd` | `CommandWheel extends CanvasLayer` | [生存] 命令轮盘手势层（spec command-wheel）：长按左键 0.65s 呼出（位移不再提前激活），小队(按空地)/攻击(按敌机)双上下文，0.3x 子弹时间；IDLE 时 set_process(false) 零常驻开销；命令经 `command_selected` 信号交回 survivor_mode（阶段 1 仅 EventLogger stub） | `setup(mode, params)` `begin_press(screen, world, target)` `end_press()`（返回 Outcome CLICK/EXECUTED/CANCELLED + 按下快照）`abort_if_pending()` `_pick_slot` `_draw_wheel` `slot_state_text`；内部类 `WheelCanvas` |
| `rts/command_wheel_params.gd` | `CommandWheelParams extends Resource` | [生存] 轮盘可调参数（去硬编码）；默认资源 `resources/command_wheel.tres` | `@export` 手势（hold 0.65s/死区 30px/一环 110px/流速 0.3）+ 紧急集合(600px)/撤离(1500px+20s)/防守(1500/500px)/火力分配(2000px/45°) |
| `survivor/radio_chatter.gd` | `RadioChatter extends CanvasLayer` | [生存] 无线电通讯显示层（spec radio-chatter），layer=19，屏幕上方"呼号 ▸ 台词"。**一次一条、绝不打断**（优先级只管排队 + 满队淘汰）；类别冷却防刷屏；队列上限 3；过期丢弃但 BOSS 剧本序列豁免。入队即快照呼号+Color，不持有 Aircraft 引用。唯一 `_process` 为 O(1) 计时器 | `say_text(trigger, speaker, color, text)` `say(trigger, speaker, color, fmt_args)` `say_unit(trigger, unit, fmt_args)` `say_boss_sequence(boss_id, phase, prefix)` `notify_enemy_loss()` `color_for_team(team, elite)`（static）`line_duration(text)`（static）；**零可调数值**——权重/冷却/概率/时长全部查 ChatterLines（JSON）。三层节流：全局冷却→冷却桶→概率骰，scripted 类全免 |
| `survivor/chatter_lines.gd` | `ChatterLines extends RefCounted` | [生存] 无线电台词表的**加载器**（本身零数值）。★ 全部台词/权重/冷却/概率住 `resources/chatter/radio_chatter.json`，文本三语住 `i18n/translations.csv` 的 `RADIO_*`。JSON 缺失/损坏 → 报 error 后系统降级静默，不崩游戏；静态缓存全进程解析一次 | `pick(trigger)`（选词防相邻重复）；static 查询 `kind_of` / `is_scripted` / `weight_of` / `cooldown_group_of` / `cooldown_of` / `chance_of` / `lines_of`（支持 lines_ref 复用）/ `global_ambient_cooldown` / `line_gap` / `queue_max` / `queue_stale_sec` / `duration_*` / `boss_sequence` / `type_has_voice` / `attrition_trigger` / `all_line_keys`（校验用）/ `all_trigger_ids` / `reload`；enum `Kind{SCRIPTED, AMBIENT}` |
| `ai/tactical/situation.gd` | `Situation extends RefCounted` | [P4 共享] 态势快照（~460 行）：纯数据，TacticalPlanner 输入 | `from_aircraft(ac)` 工厂；`new_for_test(d)` 测试用；`_recompute()` 几何派生 + **属性感知狗斗画像**（双方 effective G/角点 + roll/decel → BALANCED/ENERGY/TIGHT，spec engagement-discipline §C）；字段：几何 / 锁定 / 武器 / 小队 / `ai_aggression` / `strafe_pass_phase` + `attack_posture` |
| `ai/tactical/tactical_plan.gd` | `TacticalPlan extends RefCounted` | [P4 共享] 决策输出值类型（~80 行） | 13 种 `Intent` 枚举 + `WeaponMode` + `SurfacePhase`（SETUP/RUN/EGRESS）+ 字段（pursuit_pos / target_speed_kmh / afterburner / weapon_mode / allow_gun_fire / allow_missile_fire / target_altitude / trigger_extend_seconds / `strafe_pass_phase` / rationale） |
| `ai/tactical/bfm_intent.gd` | `BfmIntent extends RefCounted` | [P4 共享] 13 个 intent 纯函数（~930 行） | `cruise(s)` `waypoint_move(s, wp)` `passive_auto_fire(s)` `evade_missile(s)` `tail_chase(s)` `close_tail(s)` `lead_turn(s)` `lead_pursuit(s)` `lag_pursuit(s)` `merge_pass(s)` `wide_turn(s)` `extend_recover(s)` `ground_strafe(s)`（对面攻击 pass 状态机 SETUP/RUN/EGRESS + 姿态 STANDOFF/ASSAULT）+ 辅助 `_apply_combat_weapon` `_missile_engage_pos` `_missile_engage_speed` `_apply_squad_lateral_offset`（TIGHT 僚机横移 100m，其余 250m）`_gun_lead_point` `_surface_altitude` |
| `ai/tactical/tactical_planner.gd` | `TacticalPlanner extends RefCounted` | [P4 共享] 顶层决策入口（~375 行） | `plan(s, waypoint=INF)` 顶层 + `_apply_dogfight_energy_management`（ENERGY 守物理角点）+ 优先级决策树（evade/extend/no-target/surface/overshoot/boom-zoom/**5b.2 co-turn breaker**/slow-target/wide-turn/head-on/rear/side）；TIGHT 画像跳过非过冲脱离、半径占优 LAG→LEAD、僚机在 2×炮距解除外围角色；`_apply_weapon_lock` 后置 + hysteresis |
| `equipment/equipment_params.gd` | `EquipmentParams extends Resource` | [共享] 装备基类（武器+反制+规避统一抽象）；详见 [docs/changelogs/2026-04-28-equipment-scaffolding.md](../changelogs/2026-04-28-equipment-scaffolding.md) | `equipment_kind` 字段；虚方法 `can_fire(ac, target)` `desired_engagement(situation)` `fire(ac, target)` `update(ac, delta)` `ammo_ratio(ac)` `cooldown_ratio(ac)` |
| `equipment/engagement_preference.gd` | `EngagementPreference extends RefCounted` | [共享] 装备投票值类型 | 字段 `preferred_range_m` `preferred_intent` `needs_lock` `needs_los` `priority` `preferred_speed_kmh` `prefers_afterburner` `rationale`；工厂 `make(kind, range, intent, prio)` |
| `equipment/evasion_module.gd` | `EvasionModule extends EquipmentParams` | [共享] 规避模块基类（flare/cobra/herbst 子类） | `should_trigger(ac, missile)` `execute_evasion(ac, missile)` |
| `equipment/gun_equipment.gd` | `GunEquipment extends EquipmentParams` | [共享] 机炮装备包装器（commit 2/13）；持 `gun: GunParams` 引用 + 给 planner 投票 CLOSE_TAIL preference | `_init` 设 `equipment_kind="gun"`；`desired_engagement(s)` 返回 `EngagementPreference(intent=CLOSE_TAIL, range=max_range×0.5, priority=0.6)`；`ammo_ratio(ac)` `cooldown_ratio(ac)` |
| `equipment/rocket_equipment.gd` | `RocketEquipment extends EquipmentParams` | [共享] 火箭弹装备包装器（commit 3/13）；持 `rocket: RocketParams` 引用 | `_init` 设 `equipment_kind="rocket"`；`desired_engagement(s)` 返回 `EngagementPreference(intent=TAIL_CHASE, range=(min+max_fire)/2, priority=0.4)`；`ammo_ratio` `cooldown_ratio` |
| `equipment/missile_equipment.gd` | `MissileEquipment extends EquipmentParams` | [共享] 导弹装备包装器(commit 4/13）；持 `missile: MissileParams` + `is_secondary` 双槽标识 | `_init` 设 `equipment_kind="missile"`；`is_secondary=false` 发布到 `params.missile`，`true` 发布到 `params.secondary_missile`；`desired_engagement(s)` 仅主导弹返回 `EngagementPreference(intent=LEAD_PURSUIT, range=max_range_rear×0.6, priority=0.7)`；`ammo_ratio` `cooldown_ratio` |
| `equipment/flare_equipment.gd` | `FlareEquipment extends EvasionModule` | [共享] 热诱弹装备包装器（commit 5/13）；第一个 EvasionModule 子类 | `_init` 设 `equipment_kind="flare"`；持 `flare: FlareParams`；`should_trigger`/`execute_evasion` 暂留 base no-op，等 commit 7 接入投票后实装；`ammo_ratio` `cooldown_ratio` |
| `equipment/cobra_evasion.gd` | `CobraEvasion extends EvasionModule` | [共享] 眼镜蛇标记装备（commit 6/13）；publish 时若 equipment 含 CobraEvasion 且无 CobraManeuver 子节点，自动 add_child 挂载 | `_init` 设 `equipment_kind="cobra"`；should_trigger / execute_evasion 等 commit 7 实装 |
| `equipment/herbst_evasion.gd` | `HerbstEvasion extends EvasionModule` | [共享] 赫尔贝特轮标记装备（commit 6/13）；publish 时自动挂 HerbstManeuver 子节点 | `_init` 设 `equipment_kind="herbst"`；should_trigger / execute_evasion 等 commit 7 实装 |
| `equipment/railgun_equipment.gd` | `RailgunEquipment extends EquipmentParams` | [共享] 电磁炮（commit 8/13）；telegraph 充能 + hitscan + 闪电视觉 + 穿透 + 玩家版/敌人版双锁定时机；有效射程读 host `params.radar_range`；充能前统计有效在飞导弹 + 队友充能炮，普通目标及航母/舰船 MountTarget 均避免僚机超杀 | `team_charging_damage` `_try_start_charging` `_effective_max_range_m` `_commit_fire_solution` `_nose_lead_point` `_apply_hitscan_damage` |
| `equipment/laser_equipment.gd` | `LaserEquipment extends EquipmentParams` | [共享] 360° 激光照射器；仅对空，飞机压向动态失速 90%，导弹每秒损失 35% 最大速度且 G×0.5、降至 20% 摧毁；保留伤害/黑客/过热与云削弱分支 | `update` `_pick_targets` `_apply_target_pressure` `_select_hack_focus` `_advance_hack` `_is_hack_eligible` |
| `equipment/esm_pod_equipment.gd` | `EsmPodEquipment extends EquipmentParams` | [共享] 战区 ESM 吊舱；2Hz 扫描 3000m 内友军并刷新锁定 ×1.5、机炮/导弹/热诱弹 reload 时间 ×0.7 的短时 meta | `update` |
| `pilot_personality.gd` | `PilotPersonality extends RefCounted` | [共享] 飞行员心理子系统：压力/态势感知/判断误差（~223 行） | `update_stress(ai, delta)` `update_situational_awareness(ai, delta)` `update_drift(ai, delta)` `effective_skill` `effective_sa` `apply_position_error` `apply_speed_error` `apply_altitude_error` |
| `combat_unit.gd` | `CombatUnit extends Node2D` | [共享] 战斗单位基类（通用接口）；`sensor_hidden/sensor_shroud_id` 是 Snowblind 传感器遮断状态，不改变物理碰撞或阵营 | `take_damage` `is_in_radar_cone` `get_altitude_tier` `is_sensor_engagement_obscured(other)` |
| `combat_unit.gd`（命中资格） | `can_accept_new_hit(kind)` | [共享] 新命中建立前的统一查询；默认允许，Aircraft 主动特殊机动 ACTIVE 时按来源拒绝 | `combat_unit.gd:230 can_accept_new_hit`；覆写 `aircraft.gd:2146 can_accept_new_hit` |
| `missile.gd` | `Missile extends Node2D` | [共享] 导弹飞行物理（PN 制导/SARH）；连锁弹头保存逐弹命中集合并在命中后切入无制导直飞 | `_physics_process` `remember_penetration_hit` `continue_after_penetration` `already_penetrated` `_start_fade_out` `_guidance_degradation` |
| `missile_manager.gd` | `MissileManager extends Node2D` | [共享] 导弹生成、命中/伤害/爆炸、连锁弹头直穿生命周期、近炸引信 AOE | `spawn_missile` `_physics_process` `_spawn_aoe` `_update_aoe_zones` `_draw` |
| `bullet_manager.gd` | `BulletManager extends Node2D` | [共享] 子弹/火箭物理+命中；X-44 普通机炮/炮舱弹快照逐目标贯穿（CIWS 排除）；另管理轰炸弹、定时空爆弹与 AOE，命中广相走 `UnitGrid` | `spawn_bullet` `spawn_rocket` `spawn_bomber_bomb` `spawn_airburst_shell` `_update_special_projectiles` `_explode_airburst_shell` `_physics_process` `_draw` |
| `bullet_manager.gd` / `missile_manager.gd`（主动机动命中门） | — | [共享] 在日志、特效、弹体销毁/引爆和伤害之前查询 `can_accept_new_hit`；子弹穿过、导弹保留目标继续飞、AOE 不登记 | `bullet_manager.gd:770 can_accept_new_hit` `missile_manager.gd:359 can_accept_new_hit` `missile_manager.gd:501 can_accept_new_hit` |
| `util/unit_grid.gd` | `UnitGrid extends RefCounted` | [共享] 命中广相：均匀网格空间哈希，把"每弹扫全场"降到"每弹扫 3×3 邻格"；大单位(NavalUnit)进 `large_units` 恒扫、小单位入格（changelog 2026-07-24 航母群掉帧） | `rebuild` `query_into` |
| `terrain_renderer.gd` | `TerrainRenderer extends Node2D` | [共享] 地形/网格/云层绘制（子节点，自动 `_draw`） | `setup(camera)` `_draw_terrain` `_draw_grid` `_get_terrain_type` |
| `weather_system.gd` | `WeatherSystem extends Node2D` | [共享] 全局天气：高空云层（随机风向 + 噪声密度场 + 漂移 + 半透明绘制）。加入 group `"weather"` 供战斗代码查询 | `setup(camera)` `sample_density(world_pos)` `is_in_cloud(world_pos)` `_draw` |
| `camera_controller.gd` | `CameraController extends Node` | [共享] 缩放/平移/hover/坐标转换 | `setup(camera)` `update_zoom(delta)` `handle_zoom_input(factor)` `screen_to_world(screen_pos)` `handle_drag(relative)` `update_hover(screen_pos, units)` |
| `main.gd` | `Main extends Node2D` | [沙盒] 沙盒主控：输入/雷达锁定/编队/LOD（地形/相机委托共享模块，~427 行） | `_update_radar_locks` `_spawn_friendly_squad` `_update_lod` |
| `squad.gd` | `Squad extends Resource` | [共享] 6 种阵型偏移计算 + 编队反应延迟常量 + 整队 engage_mode + leader_changed；`members/leader` 为结构真源，换帅/cleanup 原子恢复幸存成员 `AI.squad` 并重排 index；可选 `leader_successors` 为 ADBS 等任务编队提供确定性继任链，空链仍按最高击坠数继任 | `set_leader` `cleanup` `_sync_member_bindings` `get_formation_offset` `get_wingman_target` `cycle_formation` |
| `squad.gd`（主动机动冷却） | `active_maneuver_cooldown_s` | [共享] 玩家小队唯一 R 主动机动冷却账本；切控只换 leader，不复制或重置 | `squad.gd:40 active_maneuver_cooldown_s` |
| `squad_factory.gd` | `SquadFactory extends RefCounted` | [共享] 编队生成统一入口（2026-05-04 重构，替代 9 处散乱的 Squad.new + add_member + ai 串接） | `create(formation, engage_mode, spacing)` `register_leader(sq, leader)` `register_wingman(sq, ac, set_state=true)` `form_up(leader, wingmen, ...)` |
| `ai/escort_behavior.gd` | `EscortBehavior extends RefCounted` | [共享] 护驾行为子系统（Sentinel UAV 绕长机+拦弹+自爆）；2026-05-04 起谓词收口，执行体仍在 ai_controller._process_simple | `is_active(ai)` `cleanup_after_leader_lost(ai)` |
| `ground_unit.gd` | `GroundUnit extends CombatUnit` | [共享] 地面单位基类（机炮+雷达+移动） | `_update_movement:63` `_update_combat:131` `_update_gun:164` |
| `sam_unit.gd` | `SAMUnit extends GroundUnit` | [共享] SAM：360° 雷达 + HQ-7 导弹 | `_update_sam_missile:26` `is_in_radar_cone:54`（覆写圆形） |
| `aa_gun_unit.gd` | `AAGunUnit extends GroundUnit` | [共享] AAA：独立炮塔 + ZU-23 | `_update_turret:69` `_update_aa_target_selection:30` |
| `airburst_aa_unit.gd` | `AirburstAAUnit extends AAGunUnit` | [共享] 远距定时空爆高炮；冻结误差火控解、三连发、只伤飞机；组/单发随机偏角封顶 7°/1.5°；静态采样器由陆基与舰载 Flak 共用 | `_update_aa_target_selection:40` `_update_airburst_weapon:76` `sample_burst_solution:99` `sample_shell_solution:119` `_begin_burst:133` `_fire_airburst_shell:143` |
| `strategic_target.gd` | `StrategicTarget extends GroundUnit` | [共享] 地堡/仓库/导弹井；永久不可锁定，只接受敌对 `bomber_bomb` | `take_damage` `take_bomber_damage:34` `is_lock_immune` `_draw` |
| `radar_station.gd` | `RadarStation extends GroundUnit` | [共享] 雷达站：20km 雷达 + 数据链共享 | `_update_datalink:35` `_update_dish:29` |
| `survivor/survivor_mode.gd` | 生存模式主控 | [生存] 每 3 级普通三轴卡 + 已购当前机体专属第四槽（每机每局一次 30%，放弃即结算）；战区四类奖励即时发放、僚机最多 2 架且不加等级、ESM 挂载；其余场景初始化/操控/雷达/升级/HUD、RTS 转发与升级归属分流 | `_on_player_leveled_up` `_roll_axis_cards` `_append_signature_offer` `_distribute_upgrade` `_on_upgrade_selected` `_grant_reward_now` `_claim_wingman_reward` `_claim_weapon_reward` `_update_radar_locks` `_ensure_player_squad` `_refresh_squad_effective_stacks` |
| `survivor/survivor_mode.gd`（扩池压力场） | `_bench_force_enemy_pool_stress` | [测试] 直属 9 机对 17 架扩池敌机：Snowblind、F-22、Gripen E、Rafale、Su-57、J-20、F-15；实体无敌且跳过远距清理 | `bench/run.cmd enemy_pool_stress 20 180` |
| `survivor/survivor_mode.gd`（无头终局与看门狗） | `_bench_setup_survivor_death` `_bench_update_survivor_death` `_bench_wall_watchdog` `_bench_finish_now` | [测试] 压力场统一从地图中心起飞，避免误触边界模态框；墙钟心跳可识别异常 `SceneTree.paused`；`survivor_death` 验证长机阵亡→僚机接管→全队覆灭→Game Over | `bench/run.cmd survivor_death 10 60 Shadow` |
| `survivor/survivor_tutorial.gd` | `SurvivorTutorial extends CanvasLayer` | [生存] 新存档基础操作教程 + `tutorial.cfg` 持久化；首批轰炸机 3 杀结算。六项操作含 E 加力和双击突击，操作后条目打勾 | `should_show` `mark_done` `should_show_wingman_switch` `mark_wingman_switch_done` `notify_afterburner` `notify_assault` `notify_bomber_killed` |
| `survivor/survivor_wingman_tutorial.gd` | `SurvivorWingmanTutorial extends CanvasLayer` | [生存] 首次获得僚机时的静态固定号机提示；无 `_process`，实际数字键接管后持久化并淡出 | `_ready` `_build_ui` `_layout` `complete` |
| `survivor/survivor_mode.gd`（王牌调度） | 同上 | [生存] 240s 六队同窗；开局无放回洗牌、连续两局首队防重复、同局不重复；150s 冷却 / 540s 截止 | `_prepare_ace_run_order` `_update_ace_support_event` |
| `survivor/friendly_asset_aggro.gd` | `FriendlyAssetAggro extends RefCounted` | [生存] 玩家接近已解放机场/奖励航母时，1 Hz 激活据点并按共享配额分流有限敌机；退出迟滞、BOSS/队长/事件保护、动态 MountTarget 归组（spec friendly-asset-aggro） | `register_airfield:37` `register_carrier:42` `register_target:51` `unregister_group:62` `tick:76` `quota_for:85` |
| `tests/test_friendly_asset_aggro.gd` | `extends RefCounted` | [测试] 据点 DORMANT/ACTIVE、2000px 进入、8s 退出迟滞、H→Q 配额、重叠组总上限、友军 300/敌方 1200 船体资源隔离 | `run:10`，bench `friendly_asset_aggro` |
| `tests/test_ciws_intercept.gd` | `extends RefCounted` | [测试] 敌方航母 BOSS 四 CIWS 真实弹道审计；V1/V4/V8 孤弹各 100 次，V4 的 2/4/5/8 枚齐射各 20 次；5 枚拦截率下限 90%、8 枚下限 80%，允许 100% 拦截 | `run:16`，bench `ciws_intercept` |
| `tests/test_dogfight_growth.gd` | `extends RefCounted` | [测试] 纯机炮双 AI 狗斗成长沙盒；F-14 六档机动 build × 当前/属性感知候选 planner × 三种开局，逐帧驱动 Situation/TacticalPlanner/AircraftPhysics，统计机炮解算窗、尾位、能量与盘旋半径 | `run` `_run_duel` `_attribute_aware_plan`，bench `dogfight_growth` |
| `tests/test_target_selection.gd` | `extends RefCounted` | [测试] 目标选择 + 战场引力回归（bench `target_sel`，修复前 35 断言；G6b 新增 2 条已释放 `_current_target` 生命周期边界断言） | `run` `_test_gravity_freed_reference_safety` |
| `survivor/map_geography.gd` | `MapGeography extends RefCounted` | [生存] 地图数据 API（OSM + 手画陆地混合）| `ensure_ready` `is_on_land(pos)` `get_land_polygons` `get_land_mask_polygons` `get_road_bands` — 详见 [map-pipeline.md](map-pipeline.md) |
| `survivor/map_geography_data.gd` | `MapGeographyData extends RefCounted` | [生存] OSM 数据加载器：从 `resources/maps/tokyo_bay.json` 读入 URBAN / ROADS_* / COASTLINE / LAND_MASK | `ensure_loaded` `_unpack` |
| `survivor/map_feature_renderer.gd` | `MapFeatureRenderer extends Node2D` | [生存] 主地图渲染：Sprite2D 底图（shader 调色）+ 边缘检测 + 手画 Polygon2D 叠加 + vignette | `_ensure_basemap_loaded` `_apply_basemap_shader_params` `_draw_edge_vignette` — 全部 `@export` 参数可 Inspector 调 |
| `survivor/map_manual_background.gd` | `@tool MapManualBackground extends Node2D` | [生存/编辑器] `map_manual.tscn` 的参考底图预览：在 Godot 编辑器里实时画出 OSM 数据作为描边参考 | `_draw_land` `_draw_urban` `_draw_roads` `_draw_info_text` |
| `survivor/axis_bars_panel.gd` | `AxisBarsPanel extends Control` | [生存] 三轴量表控件（spec evolution-attribute-gates §3.2 v6）：10 分格立柱逐格点亮 + 里程碑刻度圈三态 + 轴色（SurvivorData.AXIS_COLORS）；静态绘制仅 show_state 时重绘；Tab 面板与结算坞右栏共用；"+1 轴进度"加成格=半透明+亮边叠在点数格上、柱顶显示 `点+加成`（spec skills-720-rework §1.1） | `show_state(axis_points, profile, milestone_bonus)` `_draw` |
| `survivor/tactical_map.gd` | `TacticalMap extends CanvasLayer` | [生存] 战术缩略图：底图 PNG 缩略 + 战区/玩家/敌人标记 + CRT 扫描线暗角后绘制；点战区发 `zone_selected`，点空白处发 `nav_point_selected`（RTS 巡航点 + 琥珀色航点标记），地图右键发 `nav_cleared`；点图/选战区不自动关图；左栏**三轴常驻面板**（点数/●○进度/下一档预览/路线倾向，spec evolution-attribute-gates §3.2）+ 状态块；右缘**已激活技能清单**（按轴分桶 + hover 详情框，2026-07-27 从左栏移出并修复"先建后清恒空"bug） | `_on_map_draw` `_draw_nav_marker` `_handle_map_click` `_clear_nav_marker` `_try_click_zone` `_map_to_world` `_refresh_axis_panel` `_build_axis_column` `_build_skills_panel` `_refresh_upgrades_list` `signal zone_selected` `signal nav_point_selected` `signal nav_cleared` |
| `survivor/tactical_map.gd`（里程碑数值格式） | `_fmt_milestone_bonus` / `_fmt_milestone_value` | [生存] mult 显示百分比；机炮闪避 additive 0.10 显示 +10%；G additive 0.2 保留一位小数 | `survivor/tactical_map.gd:1200 _fmt_milestone_value` |
| `survivor/zone_data.gd` | `ZoneData extends RefCounted` | [生存] 战区状态/难度/任务/奖励预抽；四类奖励整局去重，A/B 每局随机保底各一武器与次世代技能，航母第 4 抽 pity | `_init(reward_context)` `RUN_GUARANTEED_REWARD_KINDS` `REWARD_KIND_WEIGHTS` `_assign_reward` `_nextgen_candidates` `get_reward` |
| `survivor/zone_mission.gd` | `ZoneMission extends Node` | [生存] 战区任务预刷/触发/完成；对舰为 4/5/6 艘全舰 TGT，先做 40px 全轨道水域硬闸，无全水解时逐艘减护卫、最终零舰船退化为空战；已购授权后，`air/squadron` ACTIVE 生成 2/3/4 架 F-86，`ground` ACTIVE 生成 2 架纯机炮、只攻 `GroundUnit` 的 A-10，任务结束物理撤离出图 | `survivor/zone_mission.gd:698 _spawn_naval_formation` `survivor/zone_mission.gd:772 build_naval_target_roster` `survivor/zone_mission.gd:785 safe_naval_plan` `survivor/zone_mission.gd:870 _start_air_support_if_needed` `survivor/zone_mission.gd:910 _update_air_support` `survivor/zone_mission.gd:963 _try_spawn_air_support` `survivor/zone_mission.gd:1037 _create_a10_support` `survivor/zone_mission.gd:1089 _begin_air_support_egress` `survivor/zone_mission.gd:1142 _tick_air_support_egress` |
| `survivor/bomber_mission.gd` | `BomberMission extends Node` | [生存] 10Hz 集中控制阵营对称轰炸任务：航路进入→对准→5 弹释放→直线离场 | `setup:25` `_update_member` `_release_one` `_egress_point` |
| `survivor/pause_menu.gd` | `PauseMenu extends CanvasLayer` | [生存] 暂停菜单（spec pause-menu）：ESC 打开＝硬暂停（走 `Presentation.present("panel_in")`）+ 确认页「继续作战 / 返回主菜单」；layer 21 + `PROCESS_MODE_ALWAYS`（暂停期间 survivor_mode 收不到输入，故 ESC 关闭由本模块自理）；退出分支不走 dismiss（场景即销毁，解暂停交给 `Presentation.clear_all()`） | `open` `close` `is_open` `get_transition_elements` `_build_ui` `_make_button` `_on_resume_pressed` `_on_quit_pressed` `signal resumed` `signal quit_to_menu` |
| `survivor/survivor_spawner.gd` | `SurvivorSpawner extends Node` | [生存] 刷怪系统；注册表常规敌池、玩家编队规模软匹配、ADBS 护卫、Snowblind/Sentinel/F-22、共享 XP 与“逃离”技能中和 XP；含轰炸任务、战略目标和 BOSS 世界边界护栏 | `_update_spawner` `_pick_enemy_type` `_spawn_flee_escort` `_spawn_snowblind_squad` `_ensure_sentinels_escorted` `_create_enemy` `_apply_squad_xp_share` `grant_flee_neutralization_xp` `enforce_boss_world_boundary` `spawn_bomber_mission` `spawn_strategic_target` |
| `survivor/skill_hooks.gd` | `SkillHooks` 静态服务 | [生存] 低频技能事件汇合；Hunter/J36 突击态、JAM 落地触发入侵算法处决 MQ-109～112、新 FEAR 落地触发普通载人机撤退 | `try_trigger_j36_assault` `on_player_jam_landed` `on_player_fear_landed` `dispatch_on_kill` `dispatch_on_hit` |
| `survivor/enemy_pool_registry.gd` | `EnemyPoolRegistry extends RefCounted` | [生存] 常规敌池 SSOT：55 型中的可随机条目、五角色权重、解锁/退役/Token/上限/编成/冷却、最近三队防重复；提供 ADBS 护卫零 Token 过滤池与敌版参数走廊审计 | `ROWS` `eligible_rows` `escort_rows` `pick_row` `row_for_type` `audit_enemy_params` |
| `survivor/f22_multilock.gd` | `F22Multilock extends RefCounted` | [生存] F-22 队级四锁控制器；生成边沿登记，5Hz 分配不同直属目标，活跃期只推进 0.15s 队列；近压与齐射后 12s 脱离 | `register` `tick` `shutdown` `allocate_unique_targets` `_logic_step` `_update_execution` `_enter_egress` |
| `survivor/schemer_multilock.gd` | `SchemerMultilock extends RefCounted` | [生存] Gripen C/E、Rafale、F-35 共享低频多锁；只遍历登记的特殊机，team3 队级三目标或 per2 单机双目标 | `register` `has_units` `tick` `shutdown` `_plan_team_three` `_plan_per_aircraft` `_update_queues` |
| `survivor/snowblind_controller.gd` | `SnowblindController extends RefCounted` | [生存] 创建当帧注册本体并立即隐藏，编成后同帧刷新护卫；常态 5Hz 雪幕控制：直属小队入圈揭露、4500m/2s 复隐滞回、圈内敌机显隐差分与跨边界目标释放；不创建逐机节点 | `register` `refresh_now` `tick` `has_active_state` `shutdown` `next_reveal_state` `_apply_concealment` `_release_cross_boundary_targets` `_order_escape` |
| `survivor/snowblind_shroud_visual.gd` | `SnowblindShroudVisual extends RefCounted` | [生存] Snowblind 单圆形 CanvasItem：64 段静态 Polygon2D，本地坐标连续雾团/低对比雪带由 shader `TIME` 驱动，并在圆心画不可交互本体轮廓；无 `_process/_draw/queue_redraw` | `attach(host)` |
| `survivor/dock_point.gd` | `DockPoint extends Node2D` | [生存] 停靠点组件（spec zone-reward-docking）：600px 圈 + 速度≤250km/h 持续 1s → `docked` 信号；机场/航母甲板共用；减速提示/进度环绘制；`mode` 引用每帧解析当前操控机（1-4 切控安全）；720 批地勤优化=持有 `ground_crew` 时停靠判定耗时减半 | `_process` `_draw` `signal docked` 常量 `LAND_SPEED_KMH/HOLD_SEC` |
| `survivor/survivor_debug_spawn.gd` | `SurvivorDebugSpawn extends CanvasLayer` | [生存] F5 刷怪调试面板；编队下拉走正式事件入口生成六支轮换王牌，并仅为 Debug 强制显示分段血条 | `_build_ui:126` `_on_type_changed:375` `_on_spawn_pressed:405` `_start_ace_event:470` `_on_clear_pressed:548` `_on_dump_pressed:574` |
| `survivor/survivor_debug_skills.gd` | `SurvivorDebugSkills extends CanvasLayer` | [生存] F4 技能/装备调试面板：动态覆盖全技能表；不适配技能仍可强制授予（仅尊重层数上限）；装备区可直挂尾雷/忠诚僚机/火箭/QMAAM/电磁炮/激光/ESM | `_refresh` `_build_skill_row` `_on_add_skill_by_id` `_on_loadout_changed` `debug_skill_ids` `debug_can_force_upgrade` `debug_has_loadout_kind` |
| `survivor/survivor_debug_zone.gd` | `SurvivorDebugZone extends CanvasLayer` | [生存] F6 战区调试面板：战区状态/任务重刷、计时快进、跳 BOSS、立即访问机场进化树；15 项完整战区奖励下拉直发 | `_build_ui` `_on_grant_reward` `debug_reward_keys` `_apply_zone_change` `_on_skip_to_boss` `_on_fast_forward` `_on_visit_airfield` |
| `survivor/survivor_player.gd` | `SurvivorPlayer extends Node` | [生存] 经验/等级/升级应用 + 三轴属性点与里程碑应用器 + **局内武器库**（spec evolution-attribute-gates / inrun-weapon-inventory：加点跨档应用、幂等记账、evolve 后换型重放；特殊武器 railgun/laser/机尾位/QMAAM 资源引用入库、换机补挂含强化、机尾互斥） | `add_xp` `apply_upgrade` `apply_upgrade_to(target, upgrade)`（全队下发用）`strip_upgrade_from(target, upgrade)`（王牌切控剥离，白名单 ACE_FIELD_STATS）`add_axis_point(axis, profile)`（合计 8 封顶闸，spec §2.2 v9）`add_milestone_bonus(axis, profile)`（"+1 轴进度"，cap=2）`get_axis_points` `get_milestone_progress`（点+加成，gates 不走）`total_axis_points` `milestone_xp_multiplier(profile)` `apply_crossed_milestones` `reapply_all_milestones` `record_special_weapons`（进化前快照）`remount_weapons`（进化后补挂）`_apply_milestone_effect` + 字段 `axis_points` `milestone_bonus` `weapon_inventory` — **里程碑逐机记账 / 全队下发**（728 批）：记录挂飞机 meta `MILESTONE_RECORD_META`（`survivor_player.gd:242 MILESTONE_RECORD_META`）；`applied_milestones` 退化为"当前操控机那本账"的属性视图（`survivor_player.gd:93 applied_milestones`）；读写 `_milestone_record` / `_set_milestone_record`（`survivor_player.gd:245 _milestone_record`）；下发目标注入点 `milestone_targets_provider`（`survivor_player.gd:101 milestone_targets_provider`，survivor_mode 注 `_squad_members_alive`）+ `_milestone_targets()`（`survivor_player.gd:259 _milestone_targets`）；逐机 API `apply_crossed_milestones_to`（`survivor_player.gd:273 apply_crossed_milestones_to`）/ `apply_all_milestones_to`（`survivor_player.gd:289 apply_all_milestones_to`）/ `reapply_all_milestones_to`（`survivor_player.gd:303 reapply_all_milestones_to`）/ `_apply_milestone_effect_to`（`survivor_player.gd:309 _apply_milestone_effect_to`） |
| `survivor/survivor_player.gd`（策士 XP 里程碑） | `milestone_xp_multiplier(profile)` | [生存] 从策士里程碑进度动态生成玩家级 XP 乘区，避免全队逐机下发导致按编队人数重复相乘 | `survivor/survivor_player.gd:129 milestone_xp_multiplier` |
| `survivor/survivor_data.gd` | `SurvivorData extends RefCounted` | [生存] 升级表/波次常量/Token 预算/经验曲线 + 三轴里程碑与卡片轴映射；小队规模对 XP/Token/响应等级/敌编成软倾向的纯函数；4 级金卡软 pity；43 条机体专属第四槽 | `UPGRADES` `is_upgrade_available_for` `upgrade_by_id` `upgrade_scope` `upgrade_classes` `milestone_plus_list_of` `upgrade_applies_to_machine` `close_range_lock_mult` `squad_xp_multiplier` `squad_token_bonus` `response_level` `pick_enemy_formation_class` `pick_flight_size` `least_pressure_target_index` `pick_card_for_axis`（`survivor_data.gd:3115 pick_card_for_axis`）`classified_pity_weight_multiplier`（`survivor_data.gd:2642 classified_pity_weight_multiplier`）`classified_pity_next_misses`（`survivor_data.gd:2647 classified_pity_next_misses`）`make_axis_focus_card` — 专属：`SIGNATURE_OFFER_CHANCE` `signature_upgrade_id_for_aircraft` `signature_upgrade_for_aircraft` `is_signature_upgrade` `is_normal_random_candidate` `signature_offer_hit` |
| `survivor/commander_aura.gd` | `CommanderAura extends Node` | [生存] Sentinel 光环 buff + 招募 UAV | `_apply_buff:93` `_try_recruit:155` |
| `playable_aircraft.gd` | `PlayableAircraft extends Resource` | [生存] 主角档案：UI 元数据 + base_params 引用 + 生存模式调味（属性/武器/战斗/热诱弹覆盖）+ 起始僚机配置 | 全部 `@export` 字段；详见 [playable-aircraft-workflow.md](playable-aircraft-workflow.md) |
| `survivor/survivor_playable_setup.gd` | `SurvivorPlayableSetup extends RefCounted` | [生存] 把 PlayableAircraft 应用到 Aircraft 实例（替代旧的 survivor_mode 内联 buff 块） | `apply(aircraft, profile)` `deep_dup_weapons(params)` |
| `survivor/aircraft_db.gd` | `AircraftDB extends RefCounted` | [生存] 43 机档案注册表（含 EA-18G / F/A-XX；UGC 护栏 2：按 id 取档案唯一入口） | `get_profile(id)` `register(id, path_or_res)` |
| `survivor/evolution_system.gd` | `EvolutionSystem extends RefCounted` | [生存] 进化系统：读 evolution_tree.json + 整机换档案 + 品类身份；发现态由正式局调用方显式传入，bench/僚机进化不污染 AircraftCodex | `node_of` `exits_of` `node_id_for_profile` `min_level_of` `evolve(ac, node_id, is_wingman, record_discovery)`（`evolution_system.gd:145 evolve`）`class_identity_of_profile` `CLASS_IDENTITY_BY_CATEGORY` |
| `survivor/evolution_ui.gd` | `EvolutionUI extends CanvasLayer` | [生存] 战区结算规划站**三栏**：左=进化树（套 ScrollContainer）/ 中=机体详情卡 / 右=三轴量表+强化三选一；面板 PRESET_FULL_RECT（**禁手动居中**，spec §4.9.1）；PROCESS_MODE_ALWAYS 暂停可交互；权威换型成功后同步当前框/标题/详情 | `show_offer(current, exits, team_level, choices, history, axis_points, milestone_bonus)` `mark_evolution_applied(node_id, history)`（`evolution_ui.gd:241 mark_evolution_applied`）`_on_tree_node_selected(id)` signals `evolution_chosen` `upgrade_chosen` `closed` |
| `survivor/evolution_tree_view.gd` | `EvolutionTreeView extends Control` | [生存] 机体进化树视图（皇牌空战式自下而上；层间线槽正交折线/同来源分支总线，四状态批绘；亮=可进化/灰=门槛未到/金=爬线历史/**剪影？？？=从未拥有**；路线 color code + 门槛 pip + 当前机醒目金色双层框 + 顶部三轴图例，spec aircraft-evolution-tree §4.9.4） | `setup(...)`（`evolution_tree_view.gd:53 setup`）`set_current(current, history)`（`:65 set_current`）`_build_edge_routes`（`:118 _build_edge_routes`）`_orthogonal_path`（`:156 _orthogonal_path`）`_draw_edge_batch`（`:275 _draw_edge_batch`）`can_evolve(id)` `_is_revealed(id)` `_gate_gap_text(id)` `_pip_slots(gates)`（测试 evo_detail §F/G）`CAT_AXES` signal `node_selected(id)` `interactive` |
| `survivor/evolution_detail_panel.gd` | `EvolutionDetailPanel extends VBoxContainer` | [生存] 结算站机体详情卡三段：抬头（名/徽标/种类·档位·LV/武器/标签/描述）+ 机体特性（8 项属性，非当前机附**相对当前机增减**）+ 进化需求（等级门 + 全部 gates 逐条 have/need）；spec aircraft-evolution-tree §4.9.2 | `show_node(node_id, current_id, team_level, axis_points)` `_effective_stats(prof)`(static) `_can_evolve(...)`(static) `_gate_rows(nd, pts)` const `STAT_ROWS` |
| `survivor/aircraft_codex.gd` | `AircraftCodex extends RefCounted` | [生存/局外] 机体图鉴（user://aircraft_codex.cfg，跨局持久）：拥有过=永久揭示树迷雾；低频写盘 | `is_discovered(id)` `mark_discovered(id)` |
| `tests/test_sig_skills.gd` | `extends RefCounted` | [测试] 77 项机体签名技能无头验收：43 映射/第四槽 30%/前置/milestone/apply/EA-18G 共锁接线/F/A-XX 机炮击杀门与 CD | `run` `_test_offer_rules` `_test_mig31_forward_gate` `_test_new_signatures` |
| `tests/test_skill_audit.gd` | `extends RefCounted` | [测试] 全 158 条技能审计 bench `skill_audit`：直接型逐条 apply 快照、事件型消费点、一次性 dispatch、三语玩家文案、状态构筑公式与 F4/F6 Debug 数据源覆盖 | `run` `_audit_one` `_test_regression_contracts` `_test_debug_surface_coverage` |
| `tests/test_weapon_behavior.gd` | `extends RefCounted` | [测试] 武器行为 bench `weapon`：在飞导弹计伤、急转发射、连锁弹头直穿快照/命中去重、炮艇对地扫描/侧射炮口/整梭目标锁存、AI 僚机绕过战术目标锁池、四机编队对地独立齐射、炮艇与 CIWS 射界隔离、机炮本体守卫与 crank 连续性 | `run` `_test_chain_warhead_snapshot` `_test_gunship_scans_ground_units` `_test_gunship_squad_ground_volley` |
| `tests/test_player_params.gd` | `extends RefCounted` | [测试] 43 机玩家 params 验收（bench `player_params`，45 断言）：EA-18G/F/A-XX 锚点、矩阵/雷达走廊/王冠、全谱无默认特殊武器、热诱弹分档/换机继承/资源隔离 | `run` `_test_flare_tiers` `_test_flare_inheritance` `_test_shared_resource_isolation` |
| `tests/test_attribute_gates.gd` | `extends RefCounted` | [测试] 三轴收入/里程碑/卡片/具体轴门；枚举总点数≤8 的全部分配验证 43 节点、124 边、永久不可达边=0 | `run` `_test_evolution_gates`；bench `attr_gates` |
| `tests/test_evolution_detail.gd` | `extends RefCounted` | [测试] 43 节点详情/UI/pip/当前位置与 124 条正交树边验收 | `run` `_test_gate_pips` `_test_tree_routes_and_current_marker`；bench `evo_detail` |
| `tests/test_evolution_growth_benchmark.gd` | `EvolutionGrowthBenchmark extends RefCounted` | [测试] 14 分钟正常规则成长单局：4 条固定进化路线、1/3/5/9 机、固定种子、每 60 秒结算、有效战斗覆盖、KIA 剔除、XP/击杀/首次进化/具体缺轴 JSON | `configure_from_environment` `setup` `handle_level_up` `tick` `finish`；bench `evolution_growth` |
| `tools/aggregate_evolution_growth.py` | Python CLI | [审计] 聚合有效成长样本，输出 runs/group CSV、P50/P75/P90、Tier 到达、缺轴、经验补偿建议与 Markdown/JSON 结论 | `main` `group_row` `build_conclusions` `render_markdown` |
| `tools/audit_evolution_growth.py` | Python CLI | [审计] 读取正式 tree/profile/params/签名表，生成 43 机三轴静态成长 CSV/JSON/Markdown 与斗士线判定 | `main` `load_aircraft` `tier_summary` `render_report` |
| `tests/test_evolution_tree.gd` | 旧 SceneTree 脚本（未接 BenchRunner） | [测试] 进化树完整性冒烟；Agent 日常回归用 wrapper 跑 `evo_detail` + `attr_gates`，不直接启 Godot | `_initialize` |
| `ugc/contour_baker.gd` | `ContourBaker extends RefCounted` | [UGC] 涂格位图→平滑多边形烘焙（spec map-editor §3.1）：边界追踪(含孔洞/鞍点右转)→DP 抽稀(0.8×格)→Taubin 无收缩×3对→Chaikin×3(v6 平滑重做)；逆向栅格化供官方图转换 | `bake_layer(cells,w,h,cell_size,origin)` `rasterize_polygons(…,union)` `taubin_closed` `chaikin_closed` `simplify_closed` |
| `ugc/map_document.gd` | `MapDocument extends RefCounted` | [UGC] 地图文档数据层（spec map-editor §2.3/§2.4）：schema 读写/围栏钳制/style 调色板默认=官方现值/layer_dirty 懒烘焙/撤销栈 50/过新 schema 拒载 | `save_to(path)` `load_from(path)` `from_json_dict` `push_undo` `undo` `mark_dirty_and_rebake(layer)` |
| `ugc/official_map_converter.gd` | `OfficialMapConverter extends RefCounted` | [UGC] 官方图→MapDocument 一键转换（spec map-editor §3.4）：矢量 JSON 直通 + 陆地/城区并集消重叠（偶奇自洽）+ 栅格化反推 editor_cells + 战区/出生点 JSON 化 | `build()` |
| `tests/test_map_editor_core.gd` | SceneTree 脚本 | [测试] 地图编辑器阶段1核心冒烟（--script 运行）：Chaikin对拍/烘焙几何/孔洞/栅格化往返/存读闭环/围栏/撤销 7 组 | `_initialize` |
| `ugc/ugc_loader.gd` | `UgcLoader extends RefCounted` | [UGC] 地图运行时注入器（spec map-editor §4）：地理→MapGeographyData.inject_ugc+ugc_mode 偶奇陆判 / 建筑→BuildingRenderer.inject_ugc_districts / 云→WeatherSystem.apply_ugc_config；**退出 UGC 局必调 clear() 防静态残留**；战区/spawn/style 待接线 | `load_map(path)` `apply_geography(doc)` `apply_buildings(doc)` `apply_to_weather(w, doc)` `clear()` |
| `tests/test_official_map_converter.gd` | SceneTree 脚本 | [测试] 官方图转换冒烟（--script 运行）：陆判 22500 格心逐点等价/涂格反推/直通保真/存读闭环/围栏静默 5 组 | `_initialize` |
| `tests/test_ugc_loader.gd` | SceneTree 脚本 | [测试] UgcLoader 注入冒烟（--script 运行）：地理注入偶奇等价/注入量/建筑预热与官方管线零差异/云 mask 三态/clear 还原 5 组 | `_initialize` |
| `ugc/map_editor_scene.gd` | `MapEditorScene extends Node2D` | [UGC] 地图编辑器主场景（scenes/map_editor.tscn，主菜单"地图工坊"入口）：三区 UI + 工具状态机（笔刷/橡皮/矩形/圆形/三角形 v6）+ PNG 垫图导入与亮度阈值提取（v6）+ 文件生命周期 + 试飞（meta ugc_map_path → survivor_mode）；光标/笔画格子/图形预览在本节点轻量 _draw | `_unhandled_input` `_begin_stroke` `_stamp` `_begin_shape` `_commit_shape` `_do_import_image` `_do_extract` `_save` `_test_fly` `_do_autosave` |
| `ugc/map_editor_canvas.gd` | `MapEditorCanvas extends Node2D` | [UGC] 编辑器静态画布：海色/涂格图层多边形（调色板取色）/机场/道路/海岸线/建筑/战区/边界；只在数据变化时 notify_changed 重绘（守则第 1 条） | `notify_changed()` `layer_color(layer)` `_draw` |
| `tests/test_map_editor_scene_smoke.gd` | SceneTree 脚本 | [测试] 编辑器场景冒烟（--script 运行）：tscn 实例化跑 5 帧不炸/程序化涂格烘焙/存读 | `_initialize` `_process` |
| `survivor/survivor_select.gd` | 生存模式机型选择界面 | [生存] 起手四卡 F-15/A-6E/幻影 III/F-14（F-14 固定最右；spec career-shop v2：F-15 恒开，其余生涯解锁/商店购买；未解锁 = `_effective_list` 翻 locked 占位——??? 名、不加载档案、按钮只显解锁条件句；boss debug 全谱放行）；ESC 在 boss debug 路径里跳回 boss_debug_select | `PLAYABLE_LIST:21` `_effective_list` `_unlock_hint_for` `_build_aircraft_card` `_on_aircraft_selected` |
| `survivor/survivor_map_select.gd` | 生存模式地图选择界面 | [生存] 5 槽地图卡片（1 解锁 + 4 占位）；ESC→主菜单；**B → boss_debug_select** | `MAP_LIST:18` `_build_map_card` `_on_map_selected` |
| `survivor/boss_debug_select.gd` | Boss Debug — BOSS 选择界面 | [生存/Debug] 列出可测试 BOSS（与 BossRegistry.BOSS_DEFS 同步），选中 → set_meta(boss_debug_mode/_id) → survivor_select；空白无地图渲染 + 自动 15 级 + 主题化随机 build + 进场即 PRE_STAGE | `BOSS_LIST:18` `_build_boss_card` `_on_boss_selected` |
| `survivor/boss_debug_builds.gd` | `BossDebugBuilds extends RefCounted` | [生存/Debug] 主题化随机 build roller（5 主题：missile/gun/status/low_alt/high_alt）；尊重 max_stacks / requires / excludes / requires_skill | `THEMES:13` `roll_build(level, aircraft_id, params)` `roll_for_theme_name` |
| `survivor/afterburner_charge.gd` | `AfterburnerCharge extends RefCounted` | [生存] 加力模式充能资源（spec afterburner-mode，**充能制/电池模型**）：30s 满能量 / 激活中实时耗能 / 有能量即启动 / 耗尽自动关闭 / 提前关闭保留余量 / 击杀 +0.8s；启动 = 全队快照置窗口 flag，关闭对称清理；720 账本修正 `kill_charge_bonus`（+0.6s/层）与 `duration_mult`；暴风雨 I 按单次激活实际耗能 3.0 触发全窗成员超载，暴风雨 II 条件由 survivor_mode 注入免费维持/×4 充能 | `update(delta, rate_mult, storm_ii_active)` `on_kill_charge` `toggle(leader)` `_deactivate` `remaining_seconds` |
| `debug_panel.gd` | 调试面板 | [沙盒] 状态文本/飞行员信息/生成按钮 | `_get_strategy_text:266` `_get_combat_strategy:303` `_get_pilot_info:318` |
| `event_logger.gd` | EventLogger (AutoLoad) | [共享] 全局事件环形日志 + 本局战报累计 | `log_event` `dump_to_file` `tally(subject,key,n)` `reset_stats` `format_stats_summary`（F9 末尾追加 KILL/脱靶/命中率汇总）；**signal `kill_recorded`** → survivor_hud 战况栏（spec combat-feed）+ 无线电击坠/弹射/减员台词；**signal `evasion_started(callsign, team)`** → 无线电 break 呼叫（Aircraft.set_evasion_mode 上升沿 emit）；**signal `wingman_joined(callsign, team)`** → 无线电归队呼叫（SquadFactory.register_wingman emit，订阅方按开局时间过滤） |
| `callsign_db.gd` | CallsignDB (`class_name` 静态服务，非 AutoLoad) | [共享] 呼号分配、保留、回收与重置 | `allocate` / `recycle` / `reserve_permanent` / `reset` |
| `locale_manager.gd` | LocaleManager (AutoLoad) | [共享] i18n 控制：启动读 user://locale.cfg（zh/en/ja），主菜单按钮切换+持久化+重载场景 | `_ready` `set_locale_persistent(code)` `get_current_locale()` `trm(key)` — 详见 [docs/reference/i18n.md](i18n.md) |
| `audio/audio_manager.gd` | AudioManager (AutoLoad) | [共享] 音频总控：4 条 Bus（Music/SFX/UI/Radio）程序化创建，SFX 挂远距无线电效果链；32 SFX 池，屏幕外静音；双播放器 crossfade；playlist 轮播；菜单 muffle；玩家引擎环境音 | `play_music` `stop_music` `crossfade_music` `play_music_playlist` `set_music_muffled` `play_sfx_2d` `play_ui` `set_bus_volume_linear` `start_player_engine` `save_settings` — 详见 [docs/systems/audio.md](../systems/audio.md) |
| `util/perf_buckets.gd` | PerfBuckets (AutoLoad) | [共享/Debug] 1 秒滚动性能桶：耗时、调用数与瞬时值快照，供 HUD / F9 / bench 报告读取 | `tick` `count` `set_value` `avg_us_per_frame` `format_hud_lines` `format_full_dump` |
| `bench/bench_runner.gd` | BenchRunner (AutoLoad) | [测试] 解析 `--bench` / `--duration`、注册无头断言、切入隔离测试场并统一写结果与退出码；外部只能经 `bench/run.cmd` / `bench/run.sh` 启动 | `UNIT_TESTS` `_ready` `run_unit_test` `run_all_unit_tests` `bench_finish` |
| `addons/runtime_tuner/runtime_tuner.gd` | RuntimeTuner (AutoLoad) | [Debug] F10 运行时参数面板；只改当前实例，不写回 `.tres`，游戏代码不依赖它 | `FIELDS` `_toggle` `_refresh_target` `_read_field` `_write_field` |
| `audio/audio_settings_panel.gd` | `AudioSettingsPanel extends CanvasLayer` | [共享] 音频设置面板（4 条 Bus 滑条 + 静音 + 恢复默认 + 保存到 user://audio.cfg） | `open` `close_panel` |
| `meta/merit_ledger.gd` | MeritLedger (AutoLoad) | [共享] 局外货币"功勋"账本：局内 XP × 系数 → user://merit.cfg；MetaShop 从这里扣款 | `get_total` `settle_run(xp, mult)` `spend(amount)` `debug_add` `debug_reset`；常量 `SETTLE_RETREAT=1.0` `SETTLE_KIA=0.8` `SETTLE_VICTORY=1.0`；信号 `merit_changed(total, delta)` |
| `meta/merit_coin_icon.gd` | `MeritCoinIcon extends Control` | [共享] 功勋硬币徽章 — 纯 _draw 绘制（双圈 + 12 边缘齿 + 中心十字），无外部资源依赖 | `@export radius` `coin_color` |
| `meta/career_archive.gd` | CareerArchive (AutoLoad) | [共享/局外] 玩家生涯档案（user://career.cfg，spec career-archive）：分机型击坠/BOSS 接战·击败/通关等；`build_boss_history` 同时输出轮换布尔表与逐 BOSS 完整 `defeat_counts`，供通关强化层在 spawn 前读取；bench/boss_debug 不入档 | `record_*` 系列 `build_boss_history()` `is_achievement_unlocked(id)` `get_air_kill_table()` `flush` `debug_reset`；信号 `achievement_unlocked(id)` |
| `meta/enemy_codex.gd` | `EnemyCodex` | [局外] 敌人图鉴注册表 + 计数路由 + 解锁判定；Debug build 可在本次运行全解锁但不改生涯战绩 | `entries_of` `defeat_count` `encounter_count` `is_unlocked` `debug_set_unlock_all` `progress` |
| `tests/test_career_archive.gd` | `extends RefCounted` | [测试] 生涯档案无头单测 59 断言（含 BOSS history 完整击败次数 + 图鉴 Debug 覆盖不改计数），bench key `career_archive`；存档隔离走 user://career_test.cfg | `run` `_test_codex_debug_unlock` |
| `tests/test_zone_rewards.gd` | `extends RefCounted` | [测试] 战区奖励 44 项：七件武器与三档权重、六项次世代且无独立穿透弹头、开局类别保底、整局去重、航母第 4 次 pity、起始机型偏置与门控 | `run` `_test_nextgen_roster` `_test_achievement_reward_gate` `_test_airfield_zones` |
| `tests/test_spawn_pool.gd` | `extends RefCounted` | [测试] 56 型敌机路径覆盖（42 常规池 + 14 事件/BOSS/王牌专用）、常规池数学可达性与五响应截面 3 万次防重复产出率、高度/参数走廊/ADBS/Snowblind/F-22/Sentinel 回归；bench key `spawn_pool` | `run` `_test_regular_enemy_registry` `_test_enemy_type_route_coverage` `_test_regular_pool_reachability_and_rates` `_test_adbs_escort_pool` `_test_sentinel_escort_cohesion` |
| `tests/test_boss_progression.gd` | `extends RefCounted` | [测试] BOSS 通关强化分层 22 断言：次数保留、Wraith 支援数、CSG 8/12 艘护航编成、Goose 型号门控、MQ-111 减速+累计拦截，bench key `boss_progression` | `run` |
| `meta/meta_shop.gd` | MetaShop (AutoLoad) | [共享/局外] 生涯商店账本：6 学说 + 3 基础商品 + AWACS/战区制空/战区对地/王牌 F-15/机场 SAM 支援许可 + 43 条按进化 Tier 定价的机体专属许可（全购 30000）；正式局支援查购买态，非正式局 fail-open | `signature_item_id` `is_signature_owned_for_aircraft` `is_awacs_entitled`（`meta_shop.gd:283 is_awacs_entitled`）`is_zone_air_support_entitled`（`meta_shop.gd:287 is_zone_air_support_entitled`）`is_zone_ground_support_entitled`（`meta_shop.gd:290 is_zone_ground_support_entitled`）`is_ace_f15_support_entitled`（`meta_shop.gd:294 is_ace_f15_support_entitled`）`is_airfield_sam_entitled`（`meta_shop.gd:298 is_airfield_sam_entitled`）`aircraft_unlock_ok` `item_listed_ok` `doctrine_listed_ok` `is_listed` `is_owned` `buy` `is_upgrade_gated` `migrate_legacy_loadout` |
| `meta/meta_shop_ui.gd` | `extends Node2D`（scenes/meta_shop.tscn） | [局外] 生涯商店四分页【战术学说/机体专属/战场支援/机体与后勤】；已发现专属两列完整卡，未知专属六列匿名 `???`，未知路径不加载档案/技能；商品三态按钮与余额实时刷新 | `_add_page`（`meta_shop_ui.gd:89 _add_page`）`_build_signature_page`（`meta_shop_ui.gd:143 _build_signature_page`）`_build_signature_tile`（`meta_shop_ui.gd:184 _build_signature_tile`）`_build_unknown_signature_tile`（`meta_shop_ui.gd:242 _build_unknown_signature_tile`）`_build_doctrine_tile` `_build_item_tile` `_refresh` `_on_back_pressed` |
| `tests/test_meta_shop.gd` | `extends RefCounted` | [测试] 生涯商店无头单测 88 断言：基础解锁/持久化/doctrine、43 专属目录与 30000 总价、F-14 特例、五项战场支援权益（含机场 SAM 永久授权）、四分页、legacy 迁移；bench key `meta_shop` | `run` `_test_signature_catalog` `_test_support_entitlements` `_test_shop_categories` |
| `tests/test_boss_phase.gd` | `extends RefCounted` | [测试] BOSS 阶段闸门 + 全场撤离 + BOSS 无人机不计价 无头单测 29 断言（闸门跟随解锁 / 画面内物理撤离 / 画面外静默 free / 豁免名单 / 舰船地面不动 / `no_kill_reward` 含普通 UAV 对照 / CSG 机库上限 / BOSS 阶段空地 XP 总闸），bench key `boss_phase`；StubMode 替身直调 `_update_boss_phase_purge` + `_detect_kills` | `run` |
| `naval/naval_placement.gd` | `NavalPlacement extends RefCounted`（全静态） | [共享] 舰队摆位水域校验：CSG BOSS 舰队 + 战区海上任务共用。刚体整队绕圆心转 → 每艘船走一个**同心圆**，沿圆按 40 px 步长细采 `MapGeography.is_on_land`；窄水域装不下就**降级**（缩盘旋半径 → 0 = 原地驻泊） | `naval/naval_placement.gd:57 score` `naval/naval_placement.gd:98 pick_placement`（其余纯函数见同文件） |
| `naval/weapon_mount_params.gd` / `naval/weapon_mount.gd` | `WeaponMountParams Resource` / `WeaponMount RefCounted` | [共享] 舰载挂点静态参数与运行时状态；`NAVAL_FLAK` 追加在旧枚举末尾，炮组冻结解/余弹/间隔状态存于挂点实例 | `weapon_mount.gd:49 initialize` `weapon_mount.gd:66 apply_damage` |
| `naval/naval_weapons.gd` | `NavalWeapons extends RefCounted`（全静态） | [共享] 舰载武器派发；DDG Flak 2Hz 搜敌，800–5000m 内冻结预瞄解，首弹立即 + 0.25s 三连发、6s 冷却，只走 airburst 管线 | `naval_weapons.gd:56 update` `naval_weapons.gd:356 _update_naval_flak` `naval_weapons.gd:377 _find_aircraft_for_flak` `naval_weapons.gd:393 _begin_naval_flak_burst` `naval_weapons.gd:410 _fire_naval_flak_shell` |
| `naval/naval_unit.gd` | `NavalUnit extends CombatUnit` | [共享] 舰体/部件/移动/绘制；hover 显示舰载 Flak 橙红 5000m 断续包线，活挂点用四向爆芒符号 | `naval_unit.gd:177 _physics_process` `naval_unit.gd:805 _draw_flak_envelopes` `naval_unit.gd:892 _draw_alive_mount` |
| `tests/test_naval_zone_water.gd` | `extends RefCounted` | [测试] 舰队不上岸与对舰难度无头单测 26 断言：战区 E 三档 40px 复核、4/5/6 舰型构成、真实六舰实例化、全舰 TGT/旗舰速杀负例、人工缩编、深内陆 fallback、CSG 南北锚点；bench key `naval_zone_water` | `tests/test_naval_zone_water.gd:22 run` `tests/test_naval_zone_water.gd:36 _check_fleet_compositions_and_targets` `tests/test_naval_zone_water.gd:64 _check_spawned_fleet_contract` |
| `tests/test_zone_air_support.gd` | `extends RefCounted` | [测试] 第三方友军支援 46 断言：F-86 星级映射、A-10 固定两机、机场/对舰排除、F-15 实际生成、ALLY 契约、空/地目标硬门、第三方收益隔离与物理撤离；bench key `zone_air_support` | `tests/test_zone_air_support.gd:33 run` |
| `survivor/survivor_mode.gd`（战区支援压力样本） | 同上 | [bench] `zone_support_stress`：Lv15 玩家 + 常规 31 敌混编 + Sentinel/5 护卫/Aegis + 两支 3★ F-86（8 架）+ 一支 A-10（2 架），用于性能红线与实例泄漏检查 | `survivor/survivor_mode.gd:856 _bench_force_zone_support` |
| `survivor/survivor_mode.gd`（对舰压力样本） | 同上 | [bench] `naval_zone_stress`：常规 31 敌混编 + Sentinel/5 护卫 + 3★ 六舰全目标编成，玩家置于舰队交战半径并锁血，持续覆盖 VLS/CIWS/Flak 与 18 个挂点代理 | `survivor/survivor_mode.gd:888 _bench_force_naval_zone` |
| `survivor/survivor_mode.gd`（王牌截击压力样本） | 同上 | [bench] `ace_support_stress`：Lv15 玩家 + 常规 31 敌混编 + Sentinel/5 护卫 + MARATHON 五机 + 两架 ALLY F-15，覆盖真实事件入场与 46 机性能红线 | `survivor/survivor_mode.gd:4735 _bench_force_ace_support` |
| `tests/test_naval_formation.gd` | `extends RefCounted` | [测试] 舰队编队运动无头单测 10 断言（CSG 环形巡航 6 / U-turn 转速上限兜底 3 / 独立单船不变 1），bench key `naval_formation`；逐帧真步进 `NavalUnit._update_movement`，守"整支舰队原地旋转"回归 | `run` |
| `tests/test_bomber_rotor_airburst.gd` | `extends RefCounted` | [测试] 轰炸/旋翼机/空爆炮 22 断言；含 240 组三连发真实弹道审计（3000m、250m/s 横穿），以及 DDG 2VLS+1CIWS+1Flak、三连发/冷却/管线契约；bench key `bomber_rotor_airburst` | `run` `_test_airburst_aim_and_effectiveness:96` `_test_naval_flak_mount:153` |
| `tests/test_squad_command_ui.gd` | `extends RefCounted` | [测试] 小队指挥面板启动条件 + 固定号机映射 + 长机阵亡解绑竞态无头单测（26 断言），bench key `squad_cmd_ui`；守“指挥 UI 只在 F-14 出现”“换帅后数字键漂移”“继任后全队广播退化单机”回归 | `run` `_test_leader_down_repairs_squad_bindings` |
| `tests/test_waypoint_fire_control.gd` | `extends RefCounted` | [测试] 点地移动火控阶段一回归（bench `waypoint_fire`）：玩家航点 auto-fire 保留、编队僚机不写导航目标的机会发射、SARH/FAF 原质量边界、旧协同齐射旁路阻断，以及锁定/包络/在飞弹/团队超杀/敌方隔离负例 | `run` `_test_player_waypoint_keeps_auto_fire` `_test_formation_fire_without_navigation_takeover` `_test_existing_quality_thresholds` `_test_waste_prevention_gates` |
| `events/ai_directive.gd` | `AIDirective extends RefCounted` | [生存] 事件系统下发给 AI/NavalUnit 的声明式覆盖指令；存在期间 AI 跳过常规 PATROL/ENGAGE 路由 | 工厂 `fly_to(target, on_arrival)` `patrol_ring(center, radius)` `follow_path(wps, loop)` `hold_position()` `engage_target(t)` `passive()` + `is_owner_alive()` |
| `events/game_event.gd` | `GameEvent extends RefCounted` | [生存] 事件基类（一段剧本/一次刷怪/BOSS 流程）；管理 lifecycle + managed_units + 自动撤销 directive | `_start` `_update(delta)` `_finish` `set_directive(unit, d)` `clear_directive(unit)` `clear_all_directives()` `end()` |
| `events/event_director.gd` | `EventDirector extends Node` | [生存] 事件调度器（survivor_mode 子节点；非 AutoLoad）；持 mode/player/spawner 引用，每帧 tick 所有 active 事件 | `start(event)` `find_by_name(name)` `active_count()` `_physics_process` |
| `events/boss_encounter_event.gd` | `BossEncounterEvent extends GameEvent` | [生存] BOSS 战剧本：从 history 取 `defeat_counts` 并在 spawn 前注入 encounter；随后 CINEMATIC（PRE_STAGE）→ ENGAGED → VICTORY，序列缺失 fail-open | `_init(anchor, heading_deg, map_id)` `_start` `_update` `_try_play_arrival_cinematic` `_on_arrival_cinematic_done` `_enter_engaged` `_live_player` `_refresh_encounter_player` `_sync_boss_zone_to_members` `_inbound_spawn_origin` `_apply_pre_stage_directives_csg/_ace/_mother_goose` |

## ROE 全图察觉与交战规则（2026-07-12，spec global-awareness-roe）

| 文件 | 职责 | 关键入口 |
|---|---|---|
| `scripts/survivor/roe_director.gd` | ROE 指挥部：中队察觉（感知圈/事件/战区聚合/datalink + 15s 记忆）+ 姿态派生标记 + leash 纪律 + 热度账本 → hunter 配额 | `tick()` / `_run_pass()` / `_heat_step()` / 静态 `quota_for_heat()` `heat_floor_for_level()` `step_heat_value()` |
| `scripts/events/faction_transition.gd` | 动态阵营原子事务：IFF、目标/锁定、旧小队、在飞弹药、任务身份、颜色与一次性信号 | `convert()` `_detach_old_combat_state()` `_reassign_in_flight_weapons()` |
| `scripts/events/ally_force.gd` | ALLY 第三方转换兼容包装，统一委托 `FactionTransition` | `convert_aircraft()` / `convert_ground()` |
| `scripts/survivor/hacked_ally_force.gd` | 被黑 MQ 的无 process 编队账本；绑定当前操控长机、切控重定向、战斗至被击毁 | `set_leader()` `adopt()` `shutdown()` |
| `scripts/tests/test_faction_conversion.gd` | 阵营事务、真实激光黑客完成路径、强类型缓存清理、freed 继任引用、黑客护航重绑、激光互斥与 WhiteTea 仲裁定向回归 | `bench/run.cmd faction_conversion`；`_test_laser_hack_runtime_conversion()` `_test_squad_cleanup_skips_freed_successor()` |
| `scripts/tests/build_translations.gd` | 从 CSV 重建三份跟踪的 `.translation` 资源（隔离输出到 bench/results） | `bench/run.cmd i18n_build` |
| `scripts/events/awacs_support_event.gd` | AWACS 支援事件：绕当前战区南侧的跑道形轨道 + 到点（或 BOSS 解锁）撤离飞出地图 + 进离场无线电 + buff 区（锁定×3 / 导弹 G ×1.25）静态注册表 | `lock_rate_mult_for()` / `missile_g_mult_for()` / `active_awacs()` / `_pick_orbit_center()` / `_begin_egress()` / `_say()`（trigger `awacs_onstation` / `awacs_egress`）；常量 `ON_STATION_S` `EGRESS_TIMEOUT_S` `ORBIT_STANDOFF_PX` `ORBIT_HALF_SPAN_PX` `ORBIT_MARGIN_PX` `BUFF_RADIUS_PX`（逐符号行号见 [code-index.md](code-index.md) 第三方事件段） |
| `scripts/events/ace_reinforcement_event.gd` | 王牌支援中队事件；WhiteTea 2→1 时幸存机投降转 ALLY、本人无线电、完整中和 XP 与被动离场；其它队保持全灭/撤离契约 | `_start` `_update` `should_whitetea_surrender()` `_try_whitetea_surrender()` `_tick_surrender_egress()` `_handle_ace_terminal()` `_spawn_ally_support()` `_begin_withdraw()` |
| `scripts/tests/test_roe_director.gd` | ROE 单测 33 项（热度/配额对拍/姿态派生/感知门/守区 leash），bench key `roe` | `run()` |
| `scripts/tests/test_boss_hunter.gd` | BOSS 猎手化 + Wraith 角色/执行失误/队级战术 单测 117 项（PURSUE_UNIT 执行分支 / 无归巢 / **世界边缘软收容 + 40px 物理硬护栏 + ace_support 撤离负例** / KNIGHT-SNIPER 距离带 / 两个死 meta 已清 / 热诱弹 4 命 / 瞄准误差开门 / 减速迟滞锁存），bench key `boss_hunter` | `run()` `_test_ace_world_boundary()` |

改动挂点：`combat_unit.gd`（IFF API + TEAM_* 常量）· `ai_controller.gd`（`_roe_allows_scored_engage` 感知门，acquire_target 内）· `survivor_spawner.gd`（`_roe` 持有 / hunter 整队抽调 / 磁吸退役 / 线路巡逻 roll）· `survivor_data.gd`（ROE_* 常量 7 项）· `survivor_mode.gd`（`airfield_ally_plan` / `_try_deploy_airfield_sam` / `_deploy_airfield_ally_gradual`（机场基础 AA×2 + MetaShop 授权 SAM）/ `_update_ally_events` / 攻克热度钩子 / 锁定 buff 注入）· `meta_shop.gd`（机场 SAM 永久权益）· `game_constants.gd`（FactionPalette Token + 三分支色函数）· `missile.gd`+`missile_manager.gd`（awacs_g_mult 快照）· `merit_ledger.gd`（`award()`）

## 表演导演系统（2026-07-20~21，spec ui-transition；编排手册 [cinematic-authoring.md](cinematic-authoring.md)）

| 文件 | 类/类型 | 职责 | 关键入口 |
|---|---|---|---|
| `presentation/presentation_director.gd` | autoload `Presentation` | [生存] 表演导演：序列运行 + 8 通道分发（time/camera/overlay/panel/stage/actor/radio/audio）+ 双遮罩层（压暗动态取位 `min(16, panel.layer−1)` / 特效 25）+ F8 热重载；IDLE 时 `set_process(false)` 零开销；暂停期代泵镜头 | `play_cinematic(seq, ctx)` `present(panel, seq)` `dismiss(panel, seq, hide_node)` `clear_all()`（三类泄漏总闸）`bind_camera` `bind_radio` `has_sequence` `debug_replay` 信号 `sequence_finished` |
| `presentation/time_authority.gd` | `TimeAuthority extends RefCounted` | [生存] 时间请求栈：`Engine.time_scale` + `get_tree().paused` 唯一写入口；最小值获胜、同 id 覆盖；`hard_pause` 幂等写入（不早退，防外部直写后解除失效） | `request(id, scale, blend)` `release(id, blend)` `hard_pause(on)` `solve()` `tick(unscaled_delta)` `clear_all()` |
| `presentation/sequence_player.gd` | `SequencePlayer extends RefCounted` | [生存] 序列时间线运行器：step 推进/插值/瞬时判定/超时检出/强制收尾；**不认识通道**（分发归导演） | `load_sequence` `advance(ud)` `force_finish()` `is_done` `is_timed_out` `total_duration` |
| `presentation/stage_isolator.gd` | `StageIsolator extends RefCounted` | [生存] 空舞台：冻世界下淡出非演员 + 演员唤醒（ALWAYS + `visible=true` + `lod_level=0`，破离屏 LOD 藏机）；restore 重扫全场幂等；CanvasLayer 走 visible（无 modulate） | `clear(actors, extra, t)` `restore(extra, t)` `force_restore(extra)` `_set_alpha`（静态） |
| `presentation/cinematic_cast.gd` | `CinematicCast extends RefCounted` | [生存] 演员班底：通用演员为 CombatUnit（支持 NavalUnit 特写）；飞机走位全经 owner(GameEvent) 下 `AIDirective`（真物理禁 K 帧）；尾迹演出覆写（80→240 点）；演出专属视觉隐身只对 Aircraft 复位 | `bind` `echelon_ingress`（传送+清尾迹+逐机淡入）`converge`（逐机反解速度+包线钳制）`cloak_on_meet`（交汇即隐身+窗口末兜底）`scatter`（背向玩家扇面）`trail_boost/fade/restore` `release()` |
| `presentation/ease_lib.gd` | `EaseLib`（全静态） | [共享] 缓动函数表 6 条（`back_out` 过冲 ~1.10 = 弹入手感） | `apply(name, t)` + 各函数 |
| `resources/presentation/sequences.json` | 数据 | 全部序列定义（升级急刹 / 通用面板 / Wraith / CSG / Mother Goose 登场）；数值权威副本在 spec §2；编辑器 F8 热重载 | 序列名即键；BOSS 登场命名契约 `<boss_id 小写>_arrival` |
| `tests/test_presentation.gd` | 测试 | 回归门 108 断言：时间栈配平/三类泄漏/step 边界/缓动端点/错开跨度不变式/交汇速度包线/压暗层取位/命名契约双向/UI 打断 arrival 后 fail-open 接战并亮血条 | `run()`，bench key `presentation`（并入 `--bench=all`） |

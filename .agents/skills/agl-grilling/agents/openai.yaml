interface:
  display_name: "AGL Grilling"
  short_description: "Resolve AGL design decisions before spec approval"
  default_prompt: "Use $agl-grilling to stress-test this AGL design before writing its spec."
policy:
  allow_implicit_invocation: false
